# Frontend public env (no secrets)
# B0 default: mock fixtures + demo/localStorage auth (NOT production-safe).
# B1 will use NEXT_PUBLIC_API_MODE=http with A1 bearer auth.
NEXT_PUBLIC_API_MODE=mock
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
