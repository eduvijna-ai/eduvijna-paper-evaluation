# Docker infrastructure assets

Compose definitions live at the repository root (`docker-compose.yml`).

This directory holds supporting Docker assets for local CVB development:

- MinIO bucket bootstrap helpers
- Future service entrypoint scripts

Primary local stack: PostgreSQL 16, Redis 7, MinIO, FastAPI API.
