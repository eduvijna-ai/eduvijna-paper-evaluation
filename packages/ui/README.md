# EduVijna UI

Cursor B owns this package and its accessible component architecture. Components
must provide keyboard support, visible focus states, semantic markup, and testable
accessibility behavior.
