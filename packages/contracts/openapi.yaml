openapi: 3.1.0
info:
  title: EduVijna Paper Evaluation API
  version: 0.1.0
paths:
  /health:
    get:
      operationId: health
      summary: Liveness check
      responses:
        "200":
          description: API process is healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
  /ready:
    get:
      operationId: ready
      summary: Dependency readiness check
      responses:
        "200":
          description: API dependencies are ready
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
        "503":
          description: A required dependency is unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /api/v1/system/version:
    get:
      operationId: systemVersion
      summary: Build and API version
      responses:
        "200":
          description: Current application version information
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VersionResponse"
  /api/v1/auth/login:
    post:
      operationId: login
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/LoginRequest" }
      responses:
        "200":
          description: Access token
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TokenResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/auth/me:
    get:
      operationId: currentUser
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Current user and authorization context
          content:
            application/json:
              schema: { $ref: "#/components/schemas/User" }
  /api/v1/institution:
    get:
      operationId: currentInstitution
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Tenant primary institution
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Institution" }
  /api/v1/academic-years:
    get:
      operationId: listAcademicYears
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Academic years
          content:
            application/json:
              schema: { type: array, items: { $ref: "#/components/schemas/AcademicYear" } }
    post:
      operationId: createAcademicYear
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AcademicYearInput" }
      responses:
        "201":
          description: Created academic year
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AcademicYear" }
  /api/v1/academic-years/{id}:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getAcademicYear
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: Academic year, content: { application/json: { schema: { $ref: "#/components/schemas/AcademicYear" } } } }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateAcademicYear
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AcademicYearPatch" } } } }
      responses:
        "200": { description: Updated academic year, content: { application/json: { schema: { $ref: "#/components/schemas/AcademicYear" } } } }
  /api/v1/class-sections:
    get:
      operationId: listClassSections
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: Class sections, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/ClassSection" } } } } }
    post:
      operationId: createClassSection
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/ClassSectionInput" } } } }
      responses:
        "201": { description: Created class section, content: { application/json: { schema: { $ref: "#/components/schemas/ClassSection" } } } }
  /api/v1/class-sections/{id}:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getClassSection
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: Class section, content: { application/json: { schema: { $ref: "#/components/schemas/ClassSection" } } } }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateClassSection
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/ClassSectionPatch" } } } }
      responses:
        "200": { description: Updated class section, content: { application/json: { schema: { $ref: "#/components/schemas/ClassSection" } } } }
  /api/v1/students:
    get:
      operationId: listStudents
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: Students, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/Student" } } } } }
    post:
      operationId: createStudent
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/StudentInput" } } } }
      responses:
        "201": { description: Created student, content: { application/json: { schema: { $ref: "#/components/schemas/Student" } } } }
  /api/v1/students/{id}:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getStudent
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: Student, content: { application/json: { schema: { $ref: "#/components/schemas/Student" } } } }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateStudent
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/StudentPatch" } } } }
      responses:
        "200": { description: Updated student, content: { application/json: { schema: { $ref: "#/components/schemas/Student" } } } }
  /api/v1/students/import/validate:
    post:
      operationId: validateStudentImport
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties: { file: { type: string, format: binary } }
      responses:
        "200": { description: Per-row validation without student writes, content: { application/json: { schema: { $ref: "#/components/schemas/ImportValidation" } } } }
  /api/v1/students/import/commit:
    post:
      operationId: commitStudentImport
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [import_session_id]
              properties: { import_session_id: { type: string, format: uuid } }
      responses:
        "200":
          description: Import commit result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImportCommit"
        "409":
          description: Session expired/invalid or roster conflict
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /api/v1/guardians:
    get:
      operationId: listGuardians
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: Guardians, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/Guardian" } } } } }
    post:
      operationId: createGuardian
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/GuardianInput" } } } }
      responses:
        "201": { description: Created guardian, content: { application/json: { schema: { $ref: "#/components/schemas/Guardian" } } } }
  /api/v1/guardians/{id}:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getGuardian
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: Guardian, content: { application/json: { schema: { $ref: "#/components/schemas/Guardian" } } } }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateGuardian
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/GuardianPatch" } } } }
      responses:
        "200": { description: Updated guardian, content: { application/json: { schema: { $ref: "#/components/schemas/Guardian" } } } }
  /api/v1/students/{student_id}/guardians:
    parameters:
      - { name: student_id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      operationId: listStudentGuardians
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Guardians linked to the student
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/StudentGuardianLink"
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/students/{student_id}/guardians/{guardian_id}:
    parameters:
      - { name: student_id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: guardian_id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      operationId: linkStudentGuardian
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [relationship_type]
              properties: { relationship_type: { type: string } }
      responses:
        "201": { description: Guardian linked }
    delete:
      operationId: unlinkStudentGuardian
      security: [{ bearerAuth: [] }]
      responses:
        "204": { description: Guardian unlinked }
  /api/v1/curricula:
    get:
      operationId: listCurricula
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: Tenant curricula, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/Curriculum" } } } } }
    post:
      operationId: createCurriculum
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CurriculumInput" } } } }
      responses:
        "201": { description: Curriculum created, content: { application/json: { schema: { $ref: "#/components/schemas/Curriculum" } } } }
  /api/v1/curricula/{id}:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getCurriculum
      security: [{ bearerAuth: [] }]
      responses: { "200": { description: Curriculum, content: { application/json: { schema: { $ref: "#/components/schemas/Curriculum" } } } }, "404": { $ref: "#/components/responses/NotFound" } }
    patch:
      operationId: updateCurriculum
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CurriculumPatch" } } } }
      responses: { "200": { description: Curriculum updated } }
  /api/v1/curricula/{id}/tree:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getCurriculumTree
      security: [{ bearerAuth: [] }]
      responses: { "200": { description: Ordered curriculum tree, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/CurriculumNode" } } } } } }
  /api/v1/curricula/{id}/nodes:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: createCurriculumNode
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CurriculumNodeInput" } } } }
      responses: { "201": { description: Curriculum node created }, "409": { description: Duplicate or cyclic node } }
  /api/v1/curriculum-nodes/{id}:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    patch:
      operationId: updateCurriculumNode
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CurriculumNodePatch" } } } }
      responses: { "200": { description: Curriculum node updated }, "409": { description: Parent cycle } }
  /api/v1/curricula/{id}/prerequisites:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: createCurriculumPrerequisite
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/PrerequisiteInput" } } } }
      responses: { "201": { description: Prerequisite created }, "409": { description: Prerequisite cycle } }
  /api/v1/curriculum-prerequisites/{id}:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    delete:
      operationId: deleteCurriculumPrerequisite
      security: [{ bearerAuth: [] }]
      responses: { "204": { description: Prerequisite deleted } }
  /api/v1/assessments:
    get:
      operationId: listAssessments
      security: [{ bearerAuth: [] }]
      responses: { "200": { description: Tenant assessments, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/Assessment" } } } } } }
    post:
      operationId: createAssessment
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AssessmentInput" } } } }
      responses: { "201": { description: Assessment and initial version created } }
  /api/v1/assessments/{id}:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getAssessment
      security: [{ bearerAuth: [] }]
      responses: { "200": { description: Assessment, content: { application/json: { schema: { $ref: "#/components/schemas/Assessment" } } } } }
    patch:
      operationId: updateAssessment
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AssessmentPatch" } } } }
      responses: { "200": { description: DRAFT assessment updated }, "409": { description: Assessment immutable in current state } }
  /api/v1/assessments/{id}/versions:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: listAssessmentVersions
      security: [{ bearerAuth: [] }]
      responses: { "200": { description: Assessment versions } }
    post:
      operationId: createAssessmentVersion
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AssessmentVersionInput" } } } }
      responses: { "201": { description: Assessment version created } }
  /api/v1/assessments/{id}/transition:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: transitionAssessment
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, required: [to_status], properties: { to_status: { type: string, enum: [DRAFT, RUBRIC_REVIEW, READY, ACTIVE, CLOSED, ARCHIVED] } } } } } }
      responses: { "200": { description: Assessment transitioned }, "409": { description: Invalid transition or readiness failure } }
  /api/v1/assessment-versions/{id}/questions:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getQuestionTree
      security: [{ bearerAuth: [] }]
      responses: { "200": { description: Ordered question tree } }
    post:
      operationId: createQuestionVersion
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/QuestionInput" } } } }
      responses: { "201": { description: Question version created } }
  /api/v1/question-versions/{id}:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    patch:
      operationId: updateQuestionVersion
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/QuestionVersionPatch" } } } }
      responses: { "200": { description: Question version updated }, "409": { description: Not editable or cyclic } }
    delete:
      operationId: deleteQuestionVersion
      security: [{ bearerAuth: [] }]
      responses: { "204": { description: DRAFT question deleted }, "409": { description: Question not editable } }
  /api/v1/assessment-versions/{id}/marks/reconcile:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: reconcileAssessmentMarks
      security: [{ bearerAuth: [] }]
      responses: { "200": { description: Leaf-only mark reconciliation } }
  /api/v1/assessments/{id}/answer-key-versions:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: listAnswerKeyVersions
      security: [{ bearerAuth: [] }]
      responses: { "200": { description: Answer key versions } }
    post:
      operationId: createAnswerKeyVersion
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AnswerKeyVersionInput" } } } }
      responses: { "201": { description: Answer key version created } }
  /api/v1/answer-key-versions/{id}:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    patch:
      operationId: updateAnswerKeyVersion
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AnswerKeyVersionPatch" } } } }
      responses: { "200": { description: Draft answer key updated }, "409": { description: Approved version immutable } }
  /api/v1/answer-key-versions/{id}/approve:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: approveAnswerKeyVersion
      description: Requires assessment:approve.
      security: [{ bearerAuth: [] }]
      responses: { "200": { description: Answer key approved }, "409": { description: Version cannot be approved } }
  /api/v1/assessments/{id}/rubrics:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: listRubrics
      security: [{ bearerAuth: [] }]
      responses: { "200": { description: Assessment rubrics } }
    post:
      operationId: createRubric
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RubricInput"
      responses: { "201": { description: Rubric created } }
  /api/v1/rubrics/{id}/versions:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: listRubricVersions
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Rubric versions ordered by version number
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/RubricVersion"
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: createRubricVersion
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RubricVersionInput"
      responses: { "201": { description: Rubric version created } }
  /api/v1/rubric-versions/{id}:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    patch:
      operationId: updateRubricVersion
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/RubricVersionPatch" } } } }
      responses: { "200": { description: Draft rubric updated }, "409": { description: Approved version immutable } }
  /api/v1/rubric-versions/{id}/criteria:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: listRubricCriteria
      security: [{ bearerAuth: [] }]
      responses: { "200": { description: Rubric criteria } }
    post:
      operationId: createRubricCriterion
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/RubricCriterionInput" } } } }
      responses: { "201": { description: Criterion created } }
  /api/v1/rubric-versions/{id}/reconcile:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: reconcileRubric
      security: [{ bearerAuth: [] }]
      responses: { "200": { description: Rubric mark reconciliation } }
  /api/v1/rubric-criteria/{id}:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    patch:
      operationId: updateRubricCriterion
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/RubricCriterionPatch" } } } }
      responses: { "200": { description: Draft criterion updated }, "409": { description: Approved rubric immutable } }
    delete:
      operationId: deleteRubricCriterion
      security: [{ bearerAuth: [] }]
      responses: { "204": { description: Draft criterion deleted }, "409": { description: Approved rubric immutable } }
  /api/v1/rubric-versions/{id}/approve:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: approveRubricVersion
      security: [{ bearerAuth: [] }]
      responses: { "200": { description: Rubric approved }, "409": { description: Reconciliation failure } }
  /api/v1/question-versions/{id}/curriculum-mappings:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: listQuestionCurriculumMappings
      security: [{ bearerAuth: [] }]
      responses: { "200": { description: Curriculum mappings } }
    post:
      operationId: createQuestionCurriculumMapping
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CurriculumMappingInput" } } } }
      responses: { "201": { description: Mapping created } }
  /api/v1/question-curriculum-mappings/{id}:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    delete:
      operationId: deleteQuestionCurriculumMapping
      security: [{ bearerAuth: [] }]
      responses: { "204": { description: Mapping deleted } }
  /api/v1/assessment-versions/{id}/question-paper:
    post:
      operationId: uploadAssessmentQuestionPaper
      summary: Upload question paper artifact for a DRAFT assessment version (PEV-002 / PEV-069)
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/Id" }]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        "201":
          description: Assessment artifact stored after scan hook
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AssessmentArtifact" }
        "409":
          description: Version not DRAFT or question structure already exists
        "422":
          description: Validation or malware rejection
  /api/v1/assessment-versions/{id}/question-paper/parse:
    post:
      operationId: parseQuestionPaper
      summary: Enqueue AI parse of uploaded question paper into a proposed tree (PEV-002)
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/Id" }]
      responses:
        "200":
          description: Authoring AI run (QUEUED / REVIEW_REQUIRED / …)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AuthoringAiRun" }
        "409":
          description: QUESTION_PAPER_ARTIFACT_REQUIRED when no uploaded paper is linked
        "422":
          description: Artifact integrity / evidence extraction failure
        "503":
          description: AI_PROVIDER_UNAVAILABLE when AI_PROVIDER_AUTHORING is unset
  /api/v1/assessment-versions/{id}/authoring-ai-runs/latest:
    get:
      operationId: getLatestAuthoringAiRun
      summary: Latest authoring AI run for an assessment version (optional operation filter)
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/Id"
        - name: operation
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/AuthoringAiOperation"
      responses:
        "200":
          description: Latest matching authoring AI run
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AuthoringAiRun" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/assessment-artifacts/{id}:
    get:
      operationId: getAssessmentArtifact
      summary: Tenant-scoped assessment artifact metadata (no storage credentials)
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/Id" }]
      responses:
        "200":
          description: Assessment artifact metadata
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AssessmentArtifact" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/authoring-ai-runs/{id}:
    get:
      operationId: getAuthoringAiRun
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/Id" }]
      responses:
        "200":
          description: Authoring AI run
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AuthoringAiRun" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/authoring-ai-runs/{id}/question-tree-proposal:
    put:
      operationId: updateQuestionTreeProposal
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/Id" }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/QuestionTreeProposalInput" }
      responses:
        "200":
          description: Updated proposal payload
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AuthoringAiRun" }
  /api/v1/authoring-ai-runs/{id}/apply-question-tree:
    post:
      operationId: applyQuestionTree
      summary: Materialize a REVIEW_REQUIRED parse proposal into Question / QuestionVersion rows
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/Id" }]
      responses:
        "200":
          description: Applied authoring run
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AuthoringAiRun" }
  /api/v1/authoring-ai-runs/{id}/curriculum-mapping-proposal:
    put:
      operationId: updateCurriculumMappingProposal
      summary: Teacher edit of AI curriculum-mapping proposal (candidate allowlist enforced)
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/Id" }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CurriculumMappingProposalEdit" }
      responses:
        "200":
          description: Updated proposal payload (still REVIEW_REQUIRED)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AuthoringAiRun" }
        "422":
          description: AUTHORING_PROVIDER_INVALID_CURRICULUM_NODE or validation error
  /api/v1/authoring-ai-runs/{id}/apply-curriculum-mappings:
    post:
      operationId: applyCurriculumMappings
      summary: Human apply of REVIEW_REQUIRED curriculum-mapping proposal (creates canonical rows)
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/Id" }]
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ApplyCurriculumMappingsInput" }
      responses:
        "200":
          description: Applied authoring run (SUCCEEDED)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AuthoringAiRun" }
  /api/v1/ai/proposals/answer-key:
    post:
      operationId: proposeAnswerKey
      summary: B10 AI answer-key proposal (PEV-004); requires AI_PROVIDER_AUTHORING
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AnswerKeyProposalRequest" } } } }
      responses:
        "200":
          description: Authoring AI run for answer-key proposal
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AuthoringAiRun" }
        "503":
          description: AI provider unavailable; no content fabricated
  /api/v1/ai/proposals/rubric:
    post:
      operationId: proposeRubric
      summary: B10 AI rubric proposal (PEV-004); requires AI_PROVIDER_AUTHORING
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/RubricProposalRequest" } } } }
      responses:
        "200":
          description: Authoring AI run for rubric proposal
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AuthoringAiRun" }
        "503":
          description: AI provider unavailable; no content fabricated
  /api/v1/ai/proposals/curriculum-mapping:
    post:
      operationId: suggestCurriculumMapping
      summary: B10 AI curriculum-mapping suggestion; requires AI_PROVIDER_AUTHORING
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CurriculumMappingProposalRequest" } } } }
      responses:
        "200":
          description: Authoring AI run for curriculum-mapping suggestion
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AuthoringAiRun" }
        "503":
          description: AI provider unavailable; no content fabricated
  /api/v1/submissions:
    get:
      operationId: listSubmissions
      security: [{ bearerAuth: [] }]
      parameters:
        - name: assessment_id
          in: query
          required: false
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Submissions for the authenticated tenant
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Submission"
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      operationId: uploadSubmission
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/SubmissionUploadRequest"
      responses:
        "201":
          description: Submission created and queued for page normalization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Submission"
        "400": { description: Invalid or unsupported upload }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "409": { description: Duplicate source or assessment not ACTIVE }
        "413": { description: File too large }
  /api/v1/submissions/{id}:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getSubmission
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Submission detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Submission"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/submissions/{id}/pages:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: listSubmissionPages
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Normalized submission pages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SubmissionPage"
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/submissions/{id}/source:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getSubmissionSource
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Immutable raw source bytes
          content:
            application/pdf:
              schema: { type: string, format: binary }
            image/png:
              schema: { type: string, format: binary }
            image/jpeg:
              schema: { type: string, format: binary }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/submission-pages/{id}/image:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getSubmissionPageImage
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Normalized page PNG
          content:
            image/png:
              schema: { type: string, format: binary }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/submissions/{id}/identity:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getIdentityReview
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Identity review payload with manual roster candidates
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IdentityReview"
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/submissions/{id}/identity/confirm:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: confirmIdentity
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IdentityConfirmInput"
      responses:
        "200":
          description: Identity confirmed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Submission"
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/submissions/{id}/identity/unmatched:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: markIdentityUnmatched
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Identity marked unmatched
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Submission"
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/submissions/{id}/mapping/prepare:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: prepareMappingReview
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Mapping preparation enqueued / completed
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Submission" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/submissions/{id}/mapping:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getMappingWorkspace
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Live mapping workspace for the bound assessment version
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MappingWorkspace" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/submissions/{id}/mapping/finalize:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: finalizeMappingReview
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Mapping complete — READY_FOR_EVALUATION
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Submission" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/submissions/{id}/transcription/prepare:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: prepareTranscription
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Transcription prepare / retry
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TranscriptionPrepareResult" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/submissions/{id}/transcription:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getTranscriptionWorkspace
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Transcription review workspace
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TranscriptionWorkspace" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/submissions/{id}/transcription/finalize:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: finalizeTranscription
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Transcription READY — evaluation not enqueued
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TranscriptionFinalizeResult" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/answer-regions/{id}/transcription:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    put:
      operationId: putAnswerRegionTranscription
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ManualTranscriptionInput" }
      responses:
        "200":
          description: Human transcription version created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AnswerRegionTranscription" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/answer-region-transcriptions/{id}/confirm:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: confirmAnswerRegionTranscription
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Transcription confirmed without rewriting AI confidence
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AnswerRegionTranscription" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/answer-regions/{id}/crop:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getAnswerRegionCrop
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Derived PNG crop bytes
          content:
            image/png:
              schema: { type: string, format: binary }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/submissions/{id}/evaluation/prepare:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: prepareEvaluation
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Evaluation run queued
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EvaluationPrepareResult" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/submissions/{id}/evaluation:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getEvaluationWorkspace
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Evaluation review workspace
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EvaluationWorkspace" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/submissions/{id}/evaluation/finalize:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: finalizeEvaluation
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Submission APPROVED (never PUBLISHED)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EvaluationFinalizeResult" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/evaluation-runs/{id}:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getEvaluationRun
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Evaluation run with question ledger rows
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EvaluationRunDetail" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/question-evaluations/{id}:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getQuestionEvaluation
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Question evaluation ledger row
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuestionEvaluation" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/question-evaluations/{id}/accept:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: acceptQuestionEvaluation
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Accept proposed AI score
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuestionEvaluation" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/question-evaluations/{id}/override:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: overrideQuestionEvaluation
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/OverrideEvaluationInput" }
      responses:
        "200":
          description: Override with human score
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuestionEvaluation" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/question-evaluations/{id}/feedback:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: feedbackQuestionEvaluation
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FeedbackEvaluationInput" }
      responses:
        "200":
          description: Edit feedback without score change
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuestionEvaluation" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/question-evaluations/{id}/escalate:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: escalateQuestionEvaluation
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EscalateEvaluationInput" }
      responses:
        "200":
          description: Escalate question evaluation
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuestionEvaluation" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/submissions/{id}/publication/prepare:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: preparePublication
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Publication package prepared and generation queued
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PublicationPrepareResult" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/submissions/{id}/publication:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getPublicationWorkspace
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Publication workspace for reviewers
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PublicationWorkspace" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/submissions/{id}/published-result:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getPublishedResult
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Consumer published result (404 until PUBLISHED)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PublishedResultSummary" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/publication-results/{id}/regenerate:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: regeneratePublication
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: New publication version queued (not allowed when PUBLISHED)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PublicationRegenerateResult" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/publication-results/{id}/publish:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: publishPublicationResult
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Explicit publish gate — never auto-publish
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PublicationPublishResult" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/publication-results/{id}/artifacts/{artifact_type}:
    parameters:
      - { $ref: "#/components/parameters/Id" }
      - name: artifact_type
        in: path
        required: true
        schema:
          type: string
          enum:
            [
              ANNOTATED_PDF,
              STUDENT_REPORT_PDF,
              PARENT_REPORT_PDF,
              TEACHER_REPORT_PDF,
            ]
    get:
      operationId: downloadPublicationArtifact
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Immutable PDF artifact stream
          content:
            application/pdf:
              schema: { type: string, format: binary }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/publication-results/{id}/reports/{audience}:
    parameters:
      - { $ref: "#/components/parameters/Id" }
      - name: audience
        in: path
        required: true
        schema: { type: string, enum: [student, parent, teacher] }
    get:
      operationId: previewPublicationReport
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Reviewer JSON report preview (GENERATED ok)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PublicationReportPayload" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/publication-results/{id}/annotations:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: createPublicationAnnotation
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ManualAnnotationInput" }
      responses:
        "200":
          description: Manual COMMENT/HIGHLIGHT while GENERATED
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PublicationAnnotation" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/reports/student/{student_id}/assessments/{assessment_id}:
    parameters:
      - name: student_id
        in: path
        required: true
        schema: { type: string, format: uuid }
      - name: assessment_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      operationId: getStudentAudienceReport
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Latest PUBLISHED student report only
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StudentReport" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/reports/parent/{student_id}/assessments/{assessment_id}:
    parameters:
      - name: student_id
        in: path
        required: true
        schema: { type: string, format: uuid }
      - name: assessment_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      operationId: getParentAudienceReport
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Latest PUBLISHED parent report only
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ParentReport" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/reports/teacher/{student_id}/assessments/{assessment_id}:
    parameters:
      - name: student_id
        in: path
        required: true
        schema: { type: string, format: uuid }
      - name: assessment_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      operationId: getTeacherAudienceReport
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Latest PUBLISHED teacher report only
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TeacherReport" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/analytics/assessments/{id}:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getAssessmentAnalytics
      security: [{ bearerAuth: [] }]
      parameters:
        - name: pass_threshold_percent
          in: query
          required: false
          schema: { type: number, minimum: 0, maximum: 100 }
          description: Optional pass threshold; omitted → pass_threshold_percent and pass_rate are null
      responses:
        "200":
          description: Published-ledger assessment analytics (B8)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AssessmentAnalytics" }
        "400":
          description: Invalid pass_threshold_percent
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/analytics/students/{id}:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getStudentAnalytics
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Published-ledger student analytics with projected mastery signals (B8)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StudentAnalytics" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/analytics/students/{id}/mastery-evidence:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: listStudentMasteryEvidence
      security: [{ bearerAuth: [] }]
      parameters:
        - name: assessment_id
          in: query
          required: false
          schema: { type: string, format: uuid }
        - name: curriculum_id
          in: query
          required: false
          schema: { type: string, format: uuid }
        - name: curriculum_node_id
          in: query
          required: false
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Immutable mastery evidence rows with ledger provenance (B8_V1)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MasteryEvidenceList" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/analytics/published-results/{id}/prepare:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: preparePublishedResultAnalytics
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Ensure ANALYTICS job and enqueue materialization (idempotent)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AnalyticsPrepareResult" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Prepare requires PUBLISHED result
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
  /api/v1/analytics/students/{id}/mastery-state:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getStudentMasteryState
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Persisted longitudinal MasteryState (B12_V1 / PEV-037)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StudentMasteryState" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/analytics/students/{id}/mastery-trend:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getStudentMasteryTrend
      security: [{ bearerAuth: [] }]
      parameters:
        - name: curriculum_node_id
          in: query
          required: false
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Historical MasteryState snapshots ordered by effective published time (B12_V1)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StudentMasteryTrend" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/analytics/students/{id}/repeated-errors:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getStudentRepeatedErrors
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Recurring academic errors across distinct published results (B12_V1 / PEV-035)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StudentRepeatedErrors" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/analytics/students/{id}/recoverable-marks:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getStudentRecoverableMarks
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Potentially recoverable marks from final criterion ledger (B12_V1 / PEV-036)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StudentRecoverableMarks" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/analytics/students/{id}/mistake-notebook:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getStudentMistakeNotebook
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Persistent student mistake notebook (B12_V1 / PEV-038)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StudentMistakeNotebook" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/analytics/students/{id}/b12/rebuild:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: rebuildStudentB12Intelligence
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Deterministic idempotent B12 rebuild from immutable MasteryEvidence + published ledger
          content:
            application/json:
              schema: { $ref: "#/components/schemas/B12RebuildResult" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/learning/resources:
    get:
      operationId: listCurriculumResources
      security: [{ bearerAuth: [] }]
      parameters:
        - name: curriculum_id
          in: query
          required: false
          schema: { type: string, format: uuid }
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [DRAFT, APPROVED, ACTIVE, DEACTIVATED]
      responses:
        "200":
          description: Institution-approved curriculum resource catalog (B13)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CurriculumResourceList" }
    post:
      operationId: createCurriculumResource
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CurriculumResourceCreate" }
      responses:
        "201":
          description: Created DRAFT curriculum resource
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CurriculumResource" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Duplicate code or invalid content_ref
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
        "422":
          description: Validation error
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
  /api/v1/learning/resources/{id}:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getCurriculumResource
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Curriculum resource detail
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CurriculumResource" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateCurriculumResource
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CurriculumResourceUpdate" }
      responses:
        "200":
          description: Updated curriculum resource
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CurriculumResource" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Illegal status transition or open-web content_ref
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
  /api/v1/learning/resources/{id}/approve:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: approveCurriculumResource
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Resource marked APPROVED
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CurriculumResource" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Invalid status for approve
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
  /api/v1/learning/resources/{id}/activate:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: activateCurriculumResource
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Resource marked ACTIVE (assignable)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CurriculumResource" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Invalid status for activate
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
  /api/v1/learning/resources/{id}/deactivate:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: deactivateCurriculumResource
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Resource deactivated; historical assignments retained
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CurriculumResource" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Invalid status for deactivate
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
  /api/v1/learning/resources/{id}/nodes:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    put:
      operationId: replaceCurriculumResourceNodes
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CurriculumResourceNodesReplace" }
      responses:
        "200":
          description: Node mappings replaced
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CurriculumResource" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Foreign or inactive curriculum node
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
  /api/v1/learning/students/{id}:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getLearningWorkspace
      security: [{ bearerAuth: [] }]
      parameters:
        - name: curriculum_id
          in: query
          required: false
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Live learning workspace for a student (includes B13 assigned resources)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/LearningWorkspace" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/learning/students/{id}/resource-assignments:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: listStudentResourceAssignments
      security: [{ bearerAuth: [] }]
      parameters:
        - name: curriculum_id
          in: query
          required: false
          schema: { type: string, format: uuid }
        - name: status
          in: query
          required: false
          schema: { type: string, enum: [ASSIGNED, CANCELLED] }
      responses:
        "200":
          description: Student resource assignment list/history (B13)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StudentResourceAssignmentList" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: createStudentResourceAssignment
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/StudentResourceAssignmentCreate" }
      responses:
        "200":
          description: Created or idempotently reused ASSIGNED row
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StudentResourceAssignment" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Resource not ACTIVE, incompatible recommendation, or open-web rejection
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
  /api/v1/learning/resource-assignments/{id}/cancel:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: cancelStudentResourceAssignment
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Assignment cancelled; historical row retained
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StudentResourceAssignment" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Already cancelled
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
  /api/v1/learning/students/{id}/prepare:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: prepareLearningPlan
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/LearningPrepareInput" }
      responses:
        "200":
          description: Queued or reused learning plan run
          content:
            application/json:
              schema: { $ref: "#/components/schemas/LearningPrepareResult" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Evidence not ready or other learning conflict
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
  /api/v1/learning/plan-runs/{id}:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getLearningPlanRun
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Learning plan run with recommendations and path
          content:
            application/json:
              schema: { $ref: "#/components/schemas/LearningPlanRun" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/learning/plan-runs/{id}/improvement-blueprints/prepare:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: prepareImprovementBlueprint
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Queued or reused improvement blueprint
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ImprovementBlueprintPrepareResult" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Plan not ready, stale, or empty
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
  /api/v1/improvement-assessments/{id}:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getImprovementAssessment
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Improvement assessment blueprint detail
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ImprovementAssessment" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/improvement-assessments/{id}/approve:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: approveImprovementAssessment
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Blueprint approved (no reassessment created)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ImprovementBlueprintApproveResult" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Stale or not pending approval
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
  /api/v1/improvement-assessments/{id}/reject:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: rejectImprovementAssessment
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ImprovementBlueprintRejectInput" }
      responses:
        "200":
          description: Blueprint rejected
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ImprovementAssessment" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422":
          description: Invalid rejection reason
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
  /api/v1/improvement-assessments/{id}/reassessment:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: instantiateReassessment
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ReassessmentInstantiateRequest" }
      responses:
        "200":
          description: Created or idempotently reused B14 reassessment (DRAFT Assessment)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Reassessment" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Stale blueprint, not APPROVED, or already instantiated with different payload
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
        "422":
          description: Invalid item coverage or marks
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
  /api/v1/reassessments/{id}:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    get:
      operationId: getReassessment
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: B14 reassessment detail with mastery baseline/post/delta
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Reassessment" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/reassessments/{id}/b14/rebuild:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: rebuildReassessmentB14
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Idempotent B14 post/delta rebuild from existing B12 snapshots
          content:
            application/json:
              schema: { $ref: "#/components/schemas/B14RebuildResult" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Linked published result or exact B12 snapshot not ready
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
  /api/v1/quality/benchmark-datasets:
    get:
      operationId: listBenchmarkDatasets
      tags: [quality-benchmark]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Tenant benchmark datasets
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BenchmarkDatasetList" }
    post:
      operationId: createBenchmarkDataset
      tags: [quality-benchmark]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BenchmarkDatasetCreate" }
      responses:
        "200":
          description: Created benchmark dataset
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BenchmarkDataset" }
        "409":
          description: Dataset code conflict
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
  /api/v1/quality/benchmark-datasets/{dataset_id}:
    parameters:
      - name: dataset_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      operationId: getBenchmarkDataset
      tags: [quality-benchmark]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Benchmark dataset detail
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BenchmarkDataset" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/quality/benchmark-datasets/{dataset_id}/versions:
    parameters:
      - name: dataset_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      operationId: listBenchmarkVersions
      tags: [quality-benchmark]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Dataset versions
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BenchmarkVersionList" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: createBenchmarkVersion
      tags: [quality-benchmark]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BenchmarkVersionCreate" }
      responses:
        "200":
          description: Created DRAFT version with default thresholds
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BenchmarkVersion" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/quality/benchmark-versions/{version_id}:
    parameters:
      - name: version_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      operationId: getBenchmarkVersion
      tags: [quality-benchmark]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Benchmark version detail
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BenchmarkVersion" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/quality/benchmark-versions/{version_id}/eligible-sources:
    parameters:
      - name: version_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      operationId: listBenchmarkEligibleSources
      tags: [quality-benchmark]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: PUBLISHED + human-final eligible sources
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BenchmarkEligibleSources" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/quality/benchmark-versions/{version_id}/cases:
    parameters:
      - name: version_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      operationId: listBenchmarkCases
      tags: [quality-benchmark]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Gold cases for version
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BenchmarkCaseList" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: addBenchmarkCase
      tags: [quality-benchmark]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BenchmarkCaseCreate" }
      responses:
        "200":
          description: Created or idempotently reused gold case
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BenchmarkCase" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Version locked or source ineligible
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
  /api/v1/quality/benchmark-versions/{version_id}/cases/{case_id}:
    parameters:
      - name: version_id
        in: path
        required: true
        schema: { type: string, format: uuid }
      - name: case_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    delete:
      operationId: removeBenchmarkCase
      tags: [quality-benchmark]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Case removed (DRAFT only)
          content:
            application/json:
              schema:
                type: object
                required: [id, removed]
                properties:
                  id: { type: string, format: uuid }
                  removed: { type: boolean }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Version locked
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
  /api/v1/quality/benchmark-versions/{version_id}/lock:
    parameters:
      - name: version_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    post:
      operationId: lockBenchmarkVersion
      tags: [quality-benchmark]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Locked version (idempotent for same content hash)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BenchmarkVersion" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Empty version or lock conflict
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
  /api/v1/quality/benchmark-versions/{version_id}/regression-runs:
    parameters:
      - name: version_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      operationId: listBenchmarkRegressionRuns
      tags: [quality-benchmark]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Regression runs for version
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BenchmarkRegressionRunList" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: startBenchmarkRegressionRun
      tags: [quality-benchmark]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BenchmarkRegressionRunCreate" }
      responses:
        "200":
          description: Started or idempotently reused regression run
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BenchmarkRegressionRun" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Version not locked or unsupported candidate
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
  /api/v1/quality/regression-runs/{run_id}:
    parameters:
      - name: run_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      operationId: getBenchmarkRegressionRun
      tags: [quality-benchmark]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Regression run detail
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BenchmarkRegressionRun" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/quality/regression-runs/{run_id}/case-results:
    parameters:
      - name: run_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      operationId: listBenchmarkRegressionCaseResults
      tags: [quality-benchmark]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Per-case regression results
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BenchmarkRegressionCaseResultList" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/quality/regression-runs/{run_id}/gate:
    parameters:
      - name: run_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      operationId: evaluateBenchmarkReleaseGate
      tags: [quality-benchmark]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Release gate verdict
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BenchmarkGateVerdict" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/operations/grading-pools:
    get:
      operationId: listGradingPools
      tags: [enterprise-operations]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Grading pools
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GradingPoolList" }
    post:
      operationId: createGradingPool
      tags: [enterprise-operations]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/GradingPoolCreate" }
      responses:
        "200":
          description: Created pool
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GradingPool" }
  /api/v1/operations/grading-pools/{pool_id}:
    parameters:
      - name: pool_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      operationId: getGradingPool
      tags: [enterprise-operations]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Pool detail
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GradingPool" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/operations/grading-pools/{pool_id}/members:
    parameters:
      - name: pool_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    post:
      operationId: addGradingPoolMember
      tags: [enterprise-operations]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/GradingPoolMemberCreate" }
      responses:
        "200":
          description: Member added
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GradingPoolMember" }
  /api/v1/operations/grading-pools/{pool_id}/activate:
    parameters:
      - name: pool_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    post:
      operationId: activateGradingPool
      tags: [enterprise-operations]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Pool activated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GradingPool" }
  /api/v1/operations/grading-pools/{pool_id}/close:
    parameters:
      - name: pool_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    post:
      operationId: closeGradingPool
      tags: [enterprise-operations]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Pool closed
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GradingPool" }
  /api/v1/operations/grading-pools/{pool_id}/allocate:
    parameters:
      - name: pool_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    post:
      operationId: allocateGradingWork
      tags: [enterprise-operations]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/GradingAllocateRequest" }
      responses:
        "200":
          description: Allocated work items
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GradingWorkItemList" }
  /api/v1/operations/grading-pools/{pool_id}/assign:
    parameters:
      - name: pool_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    post:
      operationId: assignGradingWork
      tags: [enterprise-operations]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/GradingAssignRequest" }
      responses:
        "200":
          description: Assigned work item
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GradingWorkItem" }
  /api/v1/operations/grading-pools/{pool_id}/progress:
    parameters:
      - name: pool_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      operationId: getGradingPoolProgress
      tags: [enterprise-operations]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Pool progress
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GradingPoolProgress" }
  /api/v1/operations/grading/my-queue:
    get:
      operationId: getMyGradingQueue
      tags: [enterprise-operations]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Evaluator queue
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GradingWorkItemList" }
  /api/v1/operations/grading/work-items/{work_item_id}/start:
    parameters:
      - name: work_item_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    post:
      operationId: startGradingWorkItem
      tags: [enterprise-operations]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Work item started
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GradingWorkItem" }
  /api/v1/operations/grading/work-items/{work_item_id}/submit:
    parameters:
      - name: work_item_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    post:
      operationId: submitGradingWorkItem
      tags: [enterprise-operations]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Work item submitted
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GradingWorkItem" }
  /api/v1/operations/moderation-policies:
    get:
      operationId: listModerationPolicies
      tags: [enterprise-operations]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Moderation policies
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ModerationPolicyList" }
    post:
      operationId: createModerationPolicy
      tags: [enterprise-operations]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ModerationPolicyCreate" }
      responses:
        "200":
          description: Created policy
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ModerationPolicy" }
  /api/v1/operations/moderation-policies/{policy_id}/activate:
    parameters:
      - name: policy_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    post:
      operationId: activateModerationPolicy
      tags: [enterprise-operations]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Policy activated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ModerationPolicy" }
  /api/v1/operations/moderation-cases:
    get:
      operationId: listModerationCases
      tags: [enterprise-operations]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Moderation cases
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ModerationCaseList" }
  /api/v1/operations/moderation-cases/{case_id}:
    parameters:
      - name: case_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      operationId: getModerationCase
      tags: [enterprise-operations]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Case detail
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ModerationCase" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/operations/moderation-cases/{case_id}/decide:
    parameters:
      - name: case_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    post:
      operationId: decideModerationCase
      tags: [enterprise-operations]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ModerationDecideRequest" }
      responses:
        "200":
          description: Decision recorded
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ModerationCase" }
  /api/v1/operations/grievances:
    get:
      operationId: listGrievances
      tags: [enterprise-operations]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Grievance cases
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GrievanceCaseList" }
    post:
      operationId: createGrievance
      tags: [enterprise-operations]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/GrievanceCreateRequest" }
      responses:
        "200":
          description: Grievance created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GrievanceCase" }
  /api/v1/operations/grievances/{grievance_id}:
    parameters:
      - name: grievance_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      operationId: getGrievance
      tags: [enterprise-operations]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Grievance detail
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GrievanceCase" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/operations/grievances/{grievance_id}/accept:
    parameters:
      - name: grievance_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    post:
      operationId: acceptGrievance
      tags: [enterprise-operations]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/GrievanceDecideRequest" }
      responses:
        "200":
          description: Grievance accepted
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GrievanceCase" }
  /api/v1/operations/grievances/{grievance_id}/reject:
    parameters:
      - name: grievance_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    post:
      operationId: rejectGrievance
      tags: [enterprise-operations]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/GrievanceDecideRequest" }
      responses:
        "200":
          description: Grievance rejected
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GrievanceCase" }
  /api/v1/operations/grievances/{grievance_id}/resolve:
    parameters:
      - name: grievance_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    post:
      operationId: resolveGrievance
      tags: [enterprise-operations]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/GrievanceResolveRequest" }
      responses:
        "200":
          description: Grievance resolved
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GrievanceCase" }
  /api/v1/submission-pages/{id}/answer-regions:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    post:
      operationId: createAnswerRegion
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AnswerRegionInput" }
      responses:
        "201":
          description: Answer region created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AnswerRegion" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/answer-regions/{id}:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    patch:
      operationId: updateAnswerRegion
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AnswerRegionPatch" }
      responses:
        "200":
          description: Answer region updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AnswerRegion" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      operationId: deleteAnswerRegion
      security: [{ bearerAuth: [] }]
      responses:
        "204":
          description: Answer region deleted
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/submission-pages/{id}:
    parameters: [{ $ref: "#/components/parameters/Id" }]
    patch:
      operationId: updateSubmissionPage
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubmissionPagePatch" }
      responses:
        "200":
          description: Submission page updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SubmissionPage" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/submissions/{id}/question-mappings/{questionVersionId}:
    parameters:
      - { $ref: "#/components/parameters/Id" }
      - name: questionVersionId
        in: path
        required: true
        schema: { type: string, format: uuid }
    put:
      operationId: upsertQuestionMapping
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/QuestionMappingInput" }
      responses:
        "200":
          description: Question mapping upserted
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuestionAnswerMapping" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/submissions/{id}/question-mappings/{questionVersionId}/confirm:
    parameters:
      - { $ref: "#/components/parameters/Id" }
      - name: questionVersionId
        in: path
        required: true
        schema: { type: string, format: uuid }
    post:
      operationId: confirmQuestionMapping
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Question mapping confirmed
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuestionAnswerMapping" }
        "404": { $ref: "#/components/responses/NotFound" }
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  parameters:
    Id:
      name: id
      in: path
      required: true
      schema: { type: string, format: uuid }
  responses:
    Unauthorized:
      description: Authentication failed
      content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
    NotFound:
      description: Resource not found, including cross-tenant access
      content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
  schemas:
    StatusResponse:
      type: object
      additionalProperties: false
      required: [status]
      properties:
        status:
          type: string
          examples: [ok]
    VersionResponse:
      type: object
      additionalProperties: false
      required: [application, environment, git_sha, api_version]
      properties:
        application:
          type: string
        environment:
          type: string
        git_sha:
          type: string
        api_version:
          type: string
    ErrorEnvelope:
      $ref: "./schemas/error.schema.json"
    LoginRequest:
      type: object
      required: [email, password]
      properties:
        email: { type: string, format: email }
        password: { type: string, format: password }
        tenant_slug: { type: [string, "null"] }
    TokenResponse:
      type: object
      required: [access_token, token_type, expires_in, user]
      properties:
        access_token: { type: string }
        token_type: { type: string, const: bearer }
        expires_in: { type: integer }
        user: { $ref: "#/components/schemas/User" }
    User:
      type: object
      required: [id, tenant_id, email, display_name, status]
      properties:
        id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid }
        email: { type: string, format: email }
        display_name: { type: string }
        status: { type: string }
        roles: { type: array, items: { type: string } }
        permissions: { type: array, items: { type: string } }
    Institution:
      type: object
      required: [id, tenant_id, code, name]
      properties:
        id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid }
        code: { type: string }
        name: { type: string }
    AcademicYearInput:
      type: object
      required: [name, starts_on, ends_on]
      properties:
        name: { type: string }
        starts_on: { type: string, format: date }
        ends_on: { type: string, format: date }
        is_current: { type: boolean, default: false }
    AcademicYear:
      allOf:
        - { $ref: "#/components/schemas/AcademicYearInput" }
        - { type: object, required: [id, tenant_id, institution_id], properties: { id: { type: string, format: uuid }, tenant_id: { type: string, format: uuid }, institution_id: { type: string, format: uuid } } }
    ClassSectionInput:
      type: object
      required: [academic_year_id, name, grade_label]
      properties:
        academic_year_id: { type: string, format: uuid }
        name: { type: string }
        grade_label: { type: string }
    ClassSection:
      allOf:
        - { $ref: "#/components/schemas/ClassSectionInput" }
        - { type: object, required: [id, tenant_id], properties: { id: { type: string, format: uuid }, tenant_id: { type: string, format: uuid } } }
    StudentInput:
      type: object
      required: [student_code, full_name]
      properties:
        student_code: { type: string }
        admission_number: { type: [string, "null"] }
        roll_number: { type: [string, "null"] }
        full_name: { type: string }
        class_section_id: { type: [string, "null"], format: uuid }
        academic_year_id: { type: [string, "null"], format: uuid }
        status: { type: string, default: active }
    Student:
      allOf:
        - { $ref: "#/components/schemas/StudentInput" }
        - { type: object, required: [id, tenant_id, created_at, updated_at], properties: { id: { type: string, format: uuid }, tenant_id: { type: string, format: uuid }, created_at: { type: string, format: date-time }, updated_at: { type: string, format: date-time } } }
    GuardianInput:
      type: object
      required: [display_name]
      properties:
        display_name: { type: string }
        email: { type: [string, "null"], format: email }
        phone: { type: [string, "null"] }
    Guardian:
      allOf:
        - { $ref: "#/components/schemas/GuardianInput" }
        - { type: object, required: [id, tenant_id], properties: { id: { type: string, format: uuid }, tenant_id: { type: string, format: uuid } } }
    ImportValidation:
      type: object
      required: [import_session_id, status, valid_row_count, row_results, expires_at]
      properties:
        import_session_id: { type: string, format: uuid }
        status: { type: string, enum: [VALIDATED] }
        valid_row_count: { type: integer }
        row_results:
          type: array
          items:
            $ref: "#/components/schemas/ImportRowResult"
        expires_at: { type: string, format: date-time }
    ImportRowResult:
      type: object
      required: [row, outcome, data]
      properties:
        row: { type: integer }
        outcome:
          type: string
          enum:
            - VALID
            - DUPLICATE_IN_FILE
            - DUPLICATE_EXISTING
            - INVALID
            - UNKNOWN_CLASS
            - MISSING_REQUIRED_FIELD
        reason_code:
          type: string
          enum:
            - STUDENT_CODE_DUPLICATE_EXISTING
            - CLASS_ROLL_DUPLICATE_EXISTING
            - STUDENT_CODE_DUPLICATE_IN_FILE
            - CLASS_ROLL_DUPLICATE_IN_FILE
        data:
          type: object
          additionalProperties: { type: string }
        academic_year_id: { type: [string, "null"], format: uuid }
        class_section_id: { type: [string, "null"], format: uuid }
    ImportCommit:
      type: object
      required: [import_session_id, status, committed_count]
      properties:
        import_session_id: { type: string, format: uuid }
        status: { type: string }
        committed_count: { type: integer }
    StudentGuardianLink:
      type: object
      required: [student_id, guardian_id, display_name, relationship_type]
      properties:
        student_id: { type: string, format: uuid }
        guardian_id: { type: string, format: uuid }
        display_name: { type: string }
        email: { type: [string, "null"] }
        phone: { type: [string, "null"] }
        relationship_type: { type: string }
    Money:
      type: string
      pattern: '^\d{1,8}(\.\d{1,2})?$'
      examples: ["10.00"]
    CurriculumInput:
      type: object
      required: [code, name, version_label]
      properties:
        code: { type: string }
        name: { type: string }
        description: { type: [string, "null"] }
        academic_framework: { type: [string, "null"] }
        version_label: { type: string }
        status: { type: string, default: active }
    Curriculum:
      allOf:
        - { $ref: "#/components/schemas/CurriculumInput" }
        - { type: object, required: [id, tenant_id], properties: { id: { type: string, format: uuid }, tenant_id: { type: string, format: uuid } } }
    CurriculumNodeInput:
      type: object
      required: [node_type, code, name]
      properties:
        parent_id: { type: [string, "null"], format: uuid }
        node_type: { type: string }
        code: { type: string }
        name: { type: string }
        description: { type: [string, "null"] }
        sequence: { type: integer, default: 0 }
        metadata: { type: object, additionalProperties: true }
        status: { type: string, default: active }
    CurriculumNode:
      allOf:
        - { $ref: "#/components/schemas/CurriculumNodeInput" }
        - { type: object, required: [id, tenant_id, curriculum_id], properties: { id: { type: string, format: uuid }, tenant_id: { type: string, format: uuid }, curriculum_id: { type: string, format: uuid }, children: { type: array, items: { $ref: "#/components/schemas/CurriculumNode" } } } }
    PrerequisiteInput:
      type: object
      required: [prerequisite_node_id, dependent_node_id, relationship_type]
      properties:
        prerequisite_node_id: { type: string, format: uuid }
        dependent_node_id: { type: string, format: uuid }
        relationship_type: { type: string, enum: [REQUIRED, RECOMMENDED] }
    AssessmentInput:
      type: object
      required: [curriculum_id, code, title, assessment_type, max_marks]
      properties:
        curriculum_id: { type: string, format: uuid }
        subject_node_id: { type: [string, "null"], format: uuid }
        academic_year_id: { type: [string, "null"], format: uuid }
        class_section_id: { type: [string, "null"], format: uuid }
        code: { type: string }
        title: { type: string }
        description: { type: [string, "null"] }
        assessment_type: { type: string }
        max_marks: { $ref: "#/components/schemas/Money" }
        duration_minutes: { type: [integer, "null"] }
    Assessment:
      allOf:
        - { $ref: "#/components/schemas/AssessmentInput" }
        - { type: object, required: [id, tenant_id, status], properties: { id: { type: string, format: uuid }, tenant_id: { type: string, format: uuid }, status: { type: string } } }
    AssessmentVersionInput:
      type: object
      required: [title, max_marks]
      properties:
        title: { type: string }
        instructions: { type: [string, "null"] }
        max_marks: { $ref: "#/components/schemas/Money" }
        question_paper_artifact_id: { type: [string, "null"], format: uuid }
    QuestionInput:
      type: object
      required: [stable_code, display_label, sequence, prompt_text, max_marks, question_type, scoring_mode]
      properties:
        stable_code: { type: string }
        parent_question_version_id: { type: [string, "null"], format: uuid }
        display_label: { type: string }
        sequence: { type: integer }
        prompt_text: { type: string }
        max_marks: { $ref: "#/components/schemas/Money" }
        question_type: { type: string }
        scoring_mode: { type: string, enum: [LEAF_SCORABLE, CONTAINER_DERIVED] }
        instructions: { type: [string, "null"] }
    AnswerKeyVersionInput:
      type: object
      required: [assessment_version_id, question_version_id, answer_text, source_type]
      properties:
        assessment_version_id: { type: string, format: uuid }
        question_version_id: { type: string, format: uuid }
        answer_text: { type: string }
        structured_answer: { type: [object, "null"], additionalProperties: true }
        source_type: { type: string, enum: [TEACHER, IMPORTED], description: AI_PROPOSED is server-assigned by proposal flow only }
        status: { type: string, enum: [DRAFT, REVIEW_REQUIRED], default: DRAFT }
    RubricInput:
      type: object
      required: [question_version_id, title]
      properties:
        question_version_id: { type: string, format: uuid }
        title: { type: string }
        provenance: { type: string, default: TEACHER }
    RubricVersionInput:
      type: object
      required: [question_version_id]
      properties:
        question_version_id: { type: string, format: uuid }
        source_type: { type: string, enum: [TEACHER, IMPORTED], default: TEACHER }
        status: { type: string, enum: [DRAFT, REVIEW_REQUIRED], default: DRAFT }
    RubricVersion:
      type: object
      required:
        [
          id,
          tenant_id,
          rubric_id,
          question_version_id,
          version_number,
          status,
          source_type,
          created_by,
          created_at,
          updated_at,
        ]
      properties:
        id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid }
        rubric_id: { type: string, format: uuid }
        question_version_id: { type: string, format: uuid }
        version_number: { type: integer }
        status: { type: string, enum: [DRAFT, REVIEW_REQUIRED, APPROVED, SUPERSEDED] }
        source_type: { type: string }
        created_by: { type: string, format: uuid }
        approved_by: { type: [string, "null"], format: uuid }
        approved_at: { type: [string, "null"], format: date-time }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    RubricCriterionInput:
      type: object
      required: [criterion_code, description, max_marks, sequence, scoring_mode]
      properties:
        criterion_code: { type: string }
        description: { type: string }
        max_marks: { $ref: "#/components/schemas/Money" }
        sequence: { type: integer }
        scoring_mode: { type: string, enum: [ADDITIVE, DEDUCTIVE, ALL_OR_NOTHING] }
        partial_credit_allowed: { type: boolean, default: false }
        dependency_rule: { type: [object, "null"], additionalProperties: true }
        ecf_policy: { type: string, enum: [NONE, ALLOW_METHOD_CREDIT, CUSTOM_REVIEW] }
        unit_requirement: { type: [string, "null"] }
        precision_requirement: { type: [string, "null"] }
        required_reasoning: { type: [string, "null"] }
        accepted_equivalents: { type: array, items: {} }
        teacher_comment: { type: [string, "null"] }
    CurriculumMappingInput:
      type: object
      required: [curriculum_node_id, mapping_type]
      properties:
        curriculum_node_id: { type: string, format: uuid }
        mapping_type: { type: string, enum: [PRIMARY, SECONDARY, LEARNING_OUTCOME, SKILL] }
        weight: { oneOf: [{ $ref: "#/components/schemas/Money" }, { type: "null" }] }
    AnswerKeyVersionPatch:
      type: object
      properties:
        answer_text: { type: string }
        structured_answer: { type: [object, "null"], additionalProperties: true }
        status: { type: string, enum: [DRAFT, REVIEW_REQUIRED] }
    RubricCriterionPatch:
      type: object
      properties:
        description: { type: string }
        max_marks: { $ref: "#/components/schemas/Money" }
        sequence: { type: integer }
        scoring_mode: { type: string, enum: [ADDITIVE, DEDUCTIVE, ALL_OR_NOTHING] }
        partial_credit_allowed: { type: boolean }
        dependency_rule: { type: [object, "null"], additionalProperties: true }
        ecf_policy: { type: string, enum: [NONE, ALLOW_METHOD_CREDIT, CUSTOM_REVIEW] }
        unit_requirement: { type: [string, "null"] }
        precision_requirement: { type: [string, "null"] }
        required_reasoning: { type: [string, "null"] }
        accepted_equivalents: { type: array, items: {} }
        teacher_comment: { type: [string, "null"] }
    RubricVersionPatch:
      type: object
      required: [question_version_id]
      properties:
        question_version_id: { type: string, format: uuid }
        source_type: { type: string, enum: [TEACHER, IMPORTED] }
        status: { type: string, enum: [DRAFT, REVIEW_REQUIRED], default: DRAFT }
    AcademicYearPatch:
      type: object
      properties:
        name: { type: string }
        starts_on: { type: string, format: date }
        ends_on: { type: string, format: date }
        is_current: { type: boolean }
    ClassSectionPatch:
      type: object
      properties:
        academic_year_id: { type: string, format: uuid }
        name: { type: string }
        grade_label: { type: string }
    StudentPatch:
      type: object
      properties:
        student_code: { type: string }
        admission_number: { type: [string, "null"] }
        roll_number: { type: [string, "null"] }
        full_name: { type: string }
        class_section_id: { type: [string, "null"], format: uuid }
        academic_year_id: { type: [string, "null"], format: uuid }
        status: { type: string }
    GuardianPatch:
      type: object
      properties:
        display_name: { type: string }
        email: { type: [string, "null"], format: email }
        phone: { type: [string, "null"] }
    CurriculumPatch:
      type: object
      properties:
        name: { type: string }
        description: { type: [string, "null"] }
        academic_framework: { type: [string, "null"] }
        version_label: { type: string }
        status: { type: string }
    CurriculumNodePatch:
      type: object
      properties:
        parent_id: { type: [string, "null"], format: uuid }
        node_type: { type: string }
        name: { type: string }
        description: { type: [string, "null"] }
        sequence: { type: integer }
        metadata: { type: object, additionalProperties: true }
        status: { type: string }
    AssessmentPatch:
      type: object
      properties:
        title: { type: string }
        description: { type: [string, "null"] }
        assessment_type: { type: string }
        max_marks: { $ref: "#/components/schemas/Money" }
        duration_minutes: { type: [integer, "null"] }
    QuestionVersionPatch:
      type: object
      properties:
        parent_question_version_id: { type: [string, "null"], format: uuid }
        display_label: { type: string }
        sequence: { type: integer }
        prompt_text: { type: string }
        max_marks: { $ref: "#/components/schemas/Money" }
        question_type: { type: string }
        scoring_mode: { type: string, enum: [LEAF_SCORABLE, CONTAINER_DERIVED] }
        instructions: { type: [string, "null"] }
    AssessmentArtifact:
      type: object
      description: Immutable assessment source artifact (B10 / PEV-002)
      required:
        [
          id,
          tenant_id,
          assessment_id,
          artifact_type,
          original_filename,
          mime_type,
          byte_size,
          content_sha256,
          storage_key,
          security_scan_status,
          uploaded_at,
        ]
      properties:
        id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid }
        assessment_id: { type: string, format: uuid }
        artifact_type: { type: string, enum: [QUESTION_PAPER] }
        original_filename: { type: string }
        mime_type: { type: string }
        byte_size: { type: integer }
        content_sha256: { type: string, minLength: 64, maxLength: 64 }
        storage_key: { type: string }
        security_scan_status:
          type: string
          enum: [NOT_CONFIGURED, CLEAN, REJECTED, ERROR]
        uploaded_by: { type: [string, "null"], format: uuid }
        uploaded_at: { type: string, format: date-time }
        created_at: { type: [string, "null"], format: date-time }
    ProposedQuestionNode:
      type: object
      description: See packages/contracts/schemas/question-paper-parse.schema.json
      additionalProperties: false
      required:
        [
          stable_code,
          display_label,
          sequence,
          prompt_text,
          max_marks,
          question_type,
          scoring_mode,
        ]
      properties:
        stable_code: { type: string, minLength: 1, maxLength: 100 }
        display_label: { type: string, minLength: 1, maxLength: 100 }
        sequence: { type: integer, minimum: 0, maximum: 10000 }
        prompt_text: { type: string, minLength: 1, maxLength: 10000 }
        max_marks: { type: number, minimum: 0 }
        question_type: { type: string, minLength: 1, maxLength: 64 }
        scoring_mode: { type: string, enum: [LEAF_SCORABLE, CONTAINER_DERIVED] }
        instructions: { type: [string, "null"], maxLength: 5000 }
        children:
          type: array
          maxItems: 200
          items: { $ref: "#/components/schemas/ProposedQuestionNode" }
    QuestionTreeProposalInput:
      type: object
      additionalProperties: false
      required: [roots]
      properties:
        roots:
          type: array
          minItems: 1
          maxItems: 200
          items: { $ref: "#/components/schemas/ProposedQuestionNode" }
        notes: { type: [string, "null"], maxLength: 2000 }
    CurriculumMappingProposalEdit:
      type: object
      additionalProperties: false
      properties:
        mappings:
          type: array
          maxItems: 50
          items: { $ref: "#/components/schemas/ProposedCurriculumMapping" }
    ProposedCurriculumMapping:
      type: object
      additionalProperties: false
      required: [curriculum_node_id, mapping_type]
      properties:
        curriculum_node_id: { type: string, format: uuid }
        mapping_type:
          type: string
          enum: [PRIMARY, SECONDARY, LEARNING_OUTCOME, SKILL]
        weight: { type: [number, "null"], minimum: 0 }
        rationale: { type: [string, "null"], maxLength: 2000 }
    ApplyCurriculumMappingsInput:
      type: object
      additionalProperties: false
      properties:
        selected_indices:
          type: [array, "null"]
          items: { type: integer, minimum: 0 }
          maxItems: 50
    AuthoringAiOperation:
      type: string
      enum:
        [
          PARSE_QUESTION_PAPER,
          PROPOSE_ANSWER_KEY,
          PROPOSE_RUBRIC,
          SUGGEST_CURRICULUM_MAPPING,
        ]
    AuthoringAiRun:
      type: object
      description: Durable B10 authoring AI job (PEV-002 / PEV-004)
      required:
        [
          id,
          tenant_id,
          assessment_id,
          assessment_version_id,
          operation,
          status,
          input_hash,
          requested_by,
          requested_at,
        ]
      properties:
        id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid }
        assessment_id: { type: string, format: uuid }
        assessment_version_id: { type: string, format: uuid }
        question_version_id: { type: [string, "null"], format: uuid }
        assessment_artifact_id: { type: [string, "null"], format: uuid }
        operation:
          $ref: "#/components/schemas/AuthoringAiOperation"
        status:
          type: string
          enum:
            [QUEUED, RUNNING, REVIEW_REQUIRED, SUCCEEDED, FAILED, UNAVAILABLE]
        input_hash: { type: string }
        proposal_payload: { type: [object, "null"], additionalProperties: true }
        requested_by: { type: string, format: uuid }
        requested_at: { type: string, format: date-time }
        started_at: { type: [string, "null"], format: date-time }
        finished_at: { type: [string, "null"], format: date-time }
        celery_task_id: { type: [string, "null"] }
        answer_key_version_id: { type: [string, "null"], format: uuid }
        rubric_version_id: { type: [string, "null"], format: uuid }
        correlation_id: { type: [string, "null"], maxLength: 100 }
        failure_code: { type: [string, "null"] }
        failure_detail: { type: [string, "null"] }
        enqueue_error: { type: [string, "null"] }
        created_at: { type: [string, "null"], format: date-time }
        updated_at: { type: [string, "null"], format: date-time }
    AnswerKeyProposalRequest:
      type: object
      additionalProperties: false
      required: [question_version_id]
      properties:
        question_version_id: { type: string, format: uuid }
        assessment_version_id: { type: [string, "null"], format: uuid }
        instructions: { type: [string, "null"], maxLength: 500 }
        context:
          type: object
          additionalProperties: { type: string }
          maxProperties: 20
    RubricProposalRequest:
      type: object
      additionalProperties: false
      required: [question_version_id]
      properties:
        question_version_id: { type: string, format: uuid }
        assessment_version_id: { type: [string, "null"], format: uuid }
        instructions: { type: [string, "null"], maxLength: 500 }
        context:
          type: object
          additionalProperties: { type: string }
          maxProperties: 20
    CurriculumMappingProposalRequest:
      type: object
      additionalProperties: false
      required: [question_version_id]
      properties:
        question_version_id: { type: string, format: uuid }
        curriculum_id: { type: [string, "null"], format: uuid }
        instructions: { type: [string, "null"], maxLength: 500 }
        context:
          type: object
          additionalProperties: { type: string }
          maxProperties: 20

    SubmissionUploadRequest:
      type: object
      required: [assessment_id, file]
      properties:
        assessment_id: { type: string, format: uuid }
        bundle_name: { type: [string, "null"] }
        file: { type: string, format: binary }
    Submission:
      type: object
      required:
        [
          id,
          tenant_id,
          assessment_id,
          assessment_version_id,
          workflow_state,
          student_match_state,
          identity_confidence,
          mapping_confidence,
          source_content_sha256,
          original_filename,
          mime_type,
          byte_size,
          storage_status,
          page_count,
          uploaded_at,
          updated_at,
        ]
      properties:
        id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid }
        assessment_id: { type: string, format: uuid }
        assessment_version_id: { type: string, format: uuid }
        assessment_title: { type: [string, "null"] }
        student_id: { type: [string, "null"], format: uuid }
        student_display_name: { type: [string, "null"] }
        workflow_state:
          type: string
          enum:
            [
              UPLOADED,
              PROCESSING,
              IDENTITY_REVIEW,
              MAPPING_REVIEW,
              READY_FOR_EVALUATION,
              EVALUATING,
              EVALUATION_REVIEW,
              APPROVED,
              PUBLISHED,
              FAILED,
            ]
        student_match_state:
          type: string
          enum: [UNMATCHED, REVIEW_REQUIRED, AUTO_MATCHED, CONFIRMED]
        roll_number_detected: { type: [string, "null"] }
        name_detected: { type: [string, "null"] }
        identity_confidence: { type: number }
        mapping_confidence: { type: number }
        transcription_state:
          type: string
          enum:
            [
              NOT_STARTED,
              QUEUED,
              RUNNING,
              REVIEW_REQUIRED,
              READY,
              FAILED,
              UNAVAILABLE,
            ]
        source_storage_key: { type: string }
        source_content_sha256: { type: string }
        original_filename: { type: string }
        mime_type: { type: string }
        byte_size: { type: integer }
        storage_status: { type: string, enum: [PENDING, AVAILABLE, FAILED] }
        page_count: { type: integer }
        bundle_name: { type: [string, "null"] }
        uploaded_by: { type: string, format: uuid }
        uploaded_at: { type: string, format: date-time }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    SubmissionPage:
      type: object
      required: [id, tenant_id, submission_id, page_index, page_number, label, width, height]
      properties:
        id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid }
        submission_id: { type: string, format: uuid }
        page_index: { type: integer }
        page_number: { type: integer }
        label: { type: string }
        width: { type: integer }
        height: { type: integer }
        is_continuation: { type: boolean }
        created_at: { type: string, format: date-time }
    StudentMatchCandidate:
      type: object
      required: [student_id, display_name, external_ref, confidence, match_reasons]
      properties:
        student_id: { type: string, format: uuid }
        display_name: { type: string }
        external_ref: { type: string }
        grade: { type: string }
        section: { type: string }
        confidence: { type: number, minimum: 0, maximum: 1 }
        source_type: { type: string, enum: [AI, HUMAN] }
        match_reasons: { type: array, items: { type: string } }
    IdentityDetectedFields:
      type: object
      properties:
        name: { type: [string, "null"] }
        roll: { type: [string, "null"] }
        identity_confidence: { type: number, minimum: 0, maximum: 1 }
    IdentityReview:
      type: object
      required: [submission, pages, candidates, automated_matching_active]
      properties:
        submission: { $ref: "#/components/schemas/Submission" }
        pages:
          type: array
          items: { $ref: "#/components/schemas/SubmissionPage" }
        detected: { $ref: "#/components/schemas/IdentityDetectedFields" }
        candidates:
          type: array
          items: { $ref: "#/components/schemas/StudentMatchCandidate" }
        automated_matching_active: { type: boolean }
    IdentityConfirmInput:
      type: object
      required: [student_id]
      properties:
        student_id: { type: string, format: uuid }
    NormalizedBBox:
      type: object
      additionalProperties: false
      required: [x, y, width, height]
      properties:
        x: { type: number, minimum: 0, maximum: 1 }
        y: { type: number, minimum: 0, maximum: 1 }
        width: { type: number, exclusiveMinimum: 0, maximum: 1 }
        height: { type: number, exclusiveMinimum: 0, maximum: 1 }
    AnswerRegion:
      type: object
      required:
        [
          id,
          tenant_id,
          submission_page_id,
          page_id,
          label,
          bbox,
          region_type,
          source_type,
          detection_confidence,
          crossed_out,
          ignored,
          is_continuation,
        ]
      properties:
        id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid }
        submission_page_id: { type: string, format: uuid }
        page_id: { type: string, format: uuid }
        page_number: { type: integer }
        label: { type: string }
        bbox: { $ref: "#/components/schemas/NormalizedBBox" }
        x: { type: number }
        y: { type: number }
        width: { type: number }
        height: { type: number }
        region_type:
          type: string
          enum: [ANSWER, SCRATCH, DIAGRAM, IDENTITY]
        source_type: { type: string, enum: [HUMAN, AI] }
        detection_confidence: { type: number }
        confidence: { type: number }
        crossed_out: { type: boolean }
        ignored: { type: boolean }
        is_continuation: { type: boolean }
        transcription: { type: [string, "null"] }
        transcription_confidence: { type: number }
        question_id: { type: [string, "null"], format: uuid }
        created_by: { type: string, format: uuid }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    AnswerRegionInput:
      type: object
      additionalProperties: false
      required: [label, region_type, bbox]
      properties:
        label: { type: string, minLength: 1, maxLength: 255 }
        region_type:
          type: string
          enum: [ANSWER, SCRATCH, DIAGRAM, IDENTITY]
        bbox: { $ref: "#/components/schemas/NormalizedBBox" }
    AnswerRegionPatch:
      type: object
      additionalProperties: false
      properties:
        label: { type: string, minLength: 1, maxLength: 255 }
        region_type:
          type: string
          enum: [ANSWER, SCRATCH, DIAGRAM, IDENTITY]
        bbox: { $ref: "#/components/schemas/NormalizedBBox" }
        crossed_out: { type: boolean }
        ignored: { type: boolean }
        is_continuation: { type: boolean }
    SubmissionPagePatch:
      type: object
      additionalProperties: false
      required: [is_continuation]
      properties:
        is_continuation: { type: boolean }
    QuestionAnswerMapping:
      type: object
      required:
        [
          id,
          submission_id,
          question_id,
          question_version_id,
          disposition,
          mapping_state,
          mapped_by,
          mapping_confidence,
          region_ids,
        ]
      properties:
        id: { type: string, format: uuid }
        submission_id: { type: string, format: uuid }
        question_id: { type: string, format: uuid }
        question_version_id: { type: string, format: uuid }
        question_code: { type: string }
        disposition: { type: string, enum: [ANSWERED, BLANK] }
        mapping_state:
          type: string
          enum: [PROPOSED, REVIEW_REQUIRED, CONFIRMED]
        status:
          type: string
          enum: [PROPOSED, REVIEW_REQUIRED, CONFIRMED]
        mapped_by: { type: string, enum: [HUMAN, AI] }
        mapping_confidence: { type: number }
        confidence: { type: number }
        region_ids:
          type: array
          items: { type: string, format: uuid }
        confirmed_by: { type: [string, "null"], format: uuid }
        confirmed_at: { type: [string, "null"], format: date-time }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    QuestionMappingInput:
      type: object
      additionalProperties: false
      required: [disposition]
      properties:
        disposition: { type: string, enum: [ANSWERED, BLANK] }
        region_ids:
          type: array
          items: { type: string, format: uuid }
    MappingCompletionSummary:
      type: object
      required: [leaf_total, confirmed_count, unresolved_question_codes]
      properties:
        leaf_total: { type: integer }
        confirmed_count: { type: integer }
        unresolved_question_codes:
          type: array
          items: { type: string }
    MappingWorkspace:
      type: object
      required:
        [
          submission,
          pages,
          regions,
          questions,
          mappings,
          completion,
          assessment_version_id,
          automated_region_detection_active,
          automated_mapping_active,
        ]
      properties:
        submission: { $ref: "#/components/schemas/Submission" }
        pages:
          type: array
          items: { $ref: "#/components/schemas/SubmissionPage" }
        regions:
          type: array
          items: { $ref: "#/components/schemas/AnswerRegion" }
        questions:
          type: array
          items: { $ref: "#/components/schemas/MappingQuestionNode" }
        mappings:
          type: array
          items: { $ref: "#/components/schemas/QuestionAnswerMapping" }
        mapping:
          type: array
          items: { $ref: "#/components/schemas/MappingNodeCompat" }
        completion: { $ref: "#/components/schemas/MappingCompletionSummary" }
        assessment_version_id: { type: string, format: uuid }
        automated_region_detection_active: { type: boolean }
        automated_mapping_active: { type: boolean }
    MappingQuestionNode:
      type: object
      required:
        [
          id,
          question_version_id,
          question_id,
          code,
          prompt,
          max_mark,
          sort_order,
          scoring_mode,
          is_leaf_scorable,
        ]
      properties:
        id: { type: string, format: uuid }
        question_version_id: { type: string, format: uuid }
        question_id: { type: string, format: uuid }
        assessment_id: { type: string, format: uuid }
        parent_id: { type: [string, "null"], format: uuid }
        code: { type: string }
        prompt: { type: string }
        max_mark: { type: number }
        sort_order: { type: integer }
        scoring_mode: { type: string, enum: [LEAF_SCORABLE, CONTAINER_DERIVED] }
        is_leaf_scorable: { type: boolean }
        curriculum_node_ids:
          type: array
          items: { type: string }
        children:
          type: array
          items: { $ref: "#/components/schemas/MappingQuestionNode" }
    MappingNodeCompat:
      type: object
      required: [question_id, question_code, region_ids, confidence, status]
      properties:
        question_id: { type: string }
        question_code: { type: string }
        region_ids:
          type: array
          items: { type: string }
        confidence: { type: number }
        status:
          type: string
          enum: [PROPOSED, REVIEW_REQUIRED, CONFIRMED]
    TranscriptionSegment:
      type: object
      additionalProperties: false
      properties:
        kind:
          type: string
          enum: [TEXT, MATH, TABLE, DIAGRAM]
          default: TEXT
        step_index: { type: [integer, "null"], minimum: 0 }
        text: { type: [string, "null"], maxLength: 2000 }
        latex: { type: [string, "null"], maxLength: 5000 }
        table:
          type: [object, "null"]
          additionalProperties: false
          required: [rows]
          properties:
            rows:
              type: array
              minItems: 1
              maxItems: 40
              items:
                type: array
                minItems: 1
                maxItems: 20
                items: { type: string, maxLength: 500 }
        diagram_description: { type: [string, "null"], maxLength: 5000 }
        confidence: { type: [number, "null"], minimum: 0, maximum: 1 }
        start: { type: [number, "null"] }
        end: { type: [number, "null"] }
    AnswerRegionTranscription:
      type: object
      required:
        [
          id,
          answer_region_id,
          version_number,
          source_type,
          status,
          unreadable,
          visual_only,
        ]
      properties:
        id: { type: string, format: uuid }
        answer_region_id: { type: string, format: uuid }
        version_number: { type: integer }
        source_type: { type: string, enum: [AI, HUMAN] }
        text: { type: [string, "null"] }
        latex: { type: [string, "null"] }
        segments:
          type: array
          items: { $ref: "#/components/schemas/TranscriptionSegment" }
        transcription_confidence:
          type: [number, "null"]
          minimum: 0
          maximum: 1
        unreadable: { type: boolean }
        visual_only: { type: boolean }
        status:
          type: string
          enum: [PROPOSED, REVIEW_REQUIRED, CONFIRMED, SUPERSEDED]
        confirmed_by: { type: [string, "null"], format: uuid }
        confirmed_at: { type: [string, "null"], format: date-time }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    ManualTranscriptionInput:
      type: object
      additionalProperties: false
      properties:
        text: { type: [string, "null"], maxLength: 20000 }
        latex: { type: [string, "null"], maxLength: 20000 }
        unreadable: { type: boolean }
        visual_only: { type: boolean }
        outcome: { type: string, enum: [TRANSCRIBED, UNREADABLE, VISUAL_ONLY] }
    TranscriptionProgress:
      type: object
      required: [reviewed, required, label]
      properties:
        reviewed: { type: integer }
        required: { type: integer }
        label: { type: string }
    TranscriptionWorkspaceItem:
      type: object
      required: [question_version_id, disposition, regions, requires_transcription]
      properties:
        question_version_id: { type: string, format: uuid }
        question_label: { type: string }
        disposition: { type: string, enum: [ANSWERED, BLANK] }
        mapping_state: { type: string }
        requires_transcription: { type: boolean }
        regions:
          type: array
          items:
            type: object
            required: [id, label, region_type, source_type]
            properties:
              id: { type: string, format: uuid }
              label: { type: string }
              region_type: { type: string }
              source_type: { type: string, enum: [AI, HUMAN] }
              detection_confidence: { type: number }
              crop_url: { type: [string, "null"] }
              page_image_url: { type: [string, "null"] }
              latest_ai_proposal:
                oneOf:
                  - { $ref: "#/components/schemas/AnswerRegionTranscription" }
                  - { type: "null" }
              active_transcription:
                oneOf:
                  - { $ref: "#/components/schemas/AnswerRegionTranscription" }
                  - { type: "null" }
              requires_transcription: { type: boolean }
    TranscriptionWorkspace:
      type: object
      required:
        [
          submission_id,
          workflow_state,
          transcription_state,
          automated_transcription_active,
          progress,
          items,
        ]
      properties:
        submission_id: { type: string, format: uuid }
        workflow_state: { type: string }
        transcription_state:
          type: string
          enum:
            [
              NOT_STARTED,
              QUEUED,
              RUNNING,
              REVIEW_REQUIRED,
              READY,
              FAILED,
              UNAVAILABLE,
            ]
        automated_transcription_active: { type: boolean }
        progress: { $ref: "#/components/schemas/TranscriptionProgress" }
        items:
          type: array
          items: { $ref: "#/components/schemas/TranscriptionWorkspaceItem" }
    TranscriptionPrepareResult:
      type: object
      required: [submission_id, workflow_state, transcription_state]
      properties:
        submission_id: { type: string, format: uuid }
        workflow_state: { type: string }
        transcription_state: { type: string }
        job_id: { type: [string, "null"], format: uuid }
    TranscriptionFinalizeResult:
      type: object
      required: [submission_id, workflow_state, transcription_state, evaluation_enqueued]
      properties:
        submission_id: { type: string, format: uuid }
        workflow_state: { type: string }
        transcription_state: { type: string, enum: [READY] }
        evaluation_enqueued: { type: boolean, enum: [false] }
    PageAnalysisResult:
      type: object
      additionalProperties: false
      properties:
        proposed_zones:
          type: array
          maxItems: 50
          items:
            type: object
            required: [label, region_type, bbox, confidence]
            properties:
              label: { type: string }
              region_type: { type: string, enum: [ANSWER, SCRATCH, DIAGRAM, IDENTITY] }
              bbox: { $ref: "#/components/schemas/NormalizedBBox" }
              confidence: { type: number, minimum: 0, maximum: 1 }
              is_continuation: { type: boolean }
        printed_labels:
          type: array
          items: { type: string }
        metadata:
          type: object
          additionalProperties: { type: string }
    EvaluationPrepareResult:
      type: object
      required: [submission_id, workflow_state, evaluation_run_id, run_status]
      properties:
        submission_id: { type: string, format: uuid }
        workflow_state: { type: string }
        evaluation_run_id: { type: string, format: uuid }
        run_status: { type: string }
        job_id: { type: [string, "null"], format: uuid }
    EvaluationFinalizeResult:
      type: object
      required: [submission_id, workflow_state]
      properties:
        submission_id: { type: string, format: uuid }
        workflow_state: { type: string, enum: [APPROVED] }
        evaluation_run_id: { type: [string, "null"], format: uuid }
        run_status: { type: [string, "null"], enum: [COMPLETED, null] }
    EvaluationRun:
      type: object
      required: [id, submission_id, run_number, status, provider]
      properties:
        id: { type: string, format: uuid }
        submission_id: { type: string, format: uuid }
        assessment_id: { type: string, format: uuid }
        assessment_version_id: { type: string, format: uuid }
        run_number: { type: integer }
        status:
          type: string
          enum: [QUEUED, RUNNING, REVIEW_REQUIRED, COMPLETED, FAILED, SUPERSEDED]
        provider: { type: string }
        model: { type: [string, "null"] }
        rules_engine_version: { type: [string, "null"] }
        started_by: { type: [string, "null"], format: uuid }
        started_at: { type: [string, "null"], format: date-time }
        finished_at: { type: [string, "null"], format: date-time }
        failure_code: { type: [string, "null"] }
        failure_detail: { type: [string, "null"] }
    CriterionDecision:
      type: object
      required: [rubric_criterion_id, criterion_label, max_marks, decision]
      properties:
        id: { type: string, format: uuid }
        rubric_criterion_id: { type: string, format: uuid }
        criterion_code: { type: string }
        criterion_label: { type: string }
        max_marks: { type: number }
        proposed_marks: { type: [number, "null"] }
        final_marks: { type: [number, "null"] }
        decision:
          type: string
          enum: [AWARDED, PARTIAL, DEDUCTED, NOT_APPLICABLE, UNREADABLE]
        error_code: { type: [string, "null"] }
        deduction_reason: { type: [string, "null"] }
        step_index: { type: [integer, "null"] }
        ecf_source_criterion_id: { type: [string, "null"], format: uuid }
    QuestionEvaluation:
      type: object
      required:
        [
          id,
          evaluation_run_id,
          submission_id,
          assessment_version_id,
          question_version_id,
          max_mark,
          proposed_ai_score,
          workflow_state,
          identity_confidence,
          mapping_confidence,
          evaluation_confidence,
          math_verification_confidence,
        ]
      properties:
        id: { type: string, format: uuid }
        evaluation_run_id: { type: string, format: uuid }
        submission_id: { type: string, format: uuid }
        student_id: { type: [string, "null"], format: uuid }
        assessment_id: { type: string, format: uuid }
        assessment_version_id: { type: string, format: uuid }
        question_id: { type: string, format: uuid }
        question_version_id: { type: string, format: uuid }
        rubric_version_id: { type: string, format: uuid }
        answer_key_version_id: { type: string, format: uuid }
        mapping_id: { type: [string, "null"], format: uuid }
        answer_region_ids: { type: array, items: { type: string } }
        transcription_refs: { type: array, items: { type: object } }
        max_mark: { type: number }
        proposed_ai_score: { type: [number, "null"] }
        final_human_approved_score: { type: [number, "null"] }
        first_divergence_step: { type: [integer, "null"] }
        ecf_applied: { type: boolean }
        ecf_chain: { type: object }
        alternative_method_id: { type: [string, "null"] }
        alternative_method_label: { type: [string, "null"] }
        error_codes: { type: array, items: { type: string } }
        deduction_reasons: { type: array, items: { type: object } }
        criterion_snapshot: { type: array, items: { type: object } }
        criterion_decisions:
          type: array
          items: { $ref: "#/components/schemas/CriterionDecision" }
        identity_confidence: { type: [number, "null"], minimum: 0, maximum: 1 }
        mapping_confidence: { type: [number, "null"], minimum: 0, maximum: 1 }
        transcription_confidence: { type: [number, "null"], minimum: 0, maximum: 1 }
        evaluation_confidence: { type: [number, "null"], minimum: 0, maximum: 1 }
        math_verification_confidence: { type: [number, "null"], minimum: 0, maximum: 1 }
        workflow_state:
          type: string
          enum: [PENDING, PROPOSED, REVIEW_REQUIRED, ACCEPTED, OVERRIDDEN, ESCALATED]
        ledger_version: { type: integer }
        reviewed_by: { type: [string, "null"], format: uuid }
        reviewed_at: { type: [string, "null"], format: date-time }
        reviewer_feedback: { type: [string, "null"] }
        approved_snapshot_hash: { type: [string, "null"] }
    EvaluationRunDetail:
      allOf:
        - { $ref: "#/components/schemas/EvaluationRun" }
        - type: object
          properties:
            question_evaluations:
              type: array
              items: { $ref: "#/components/schemas/QuestionEvaluation" }
    EvaluationWorkspace:
      type: object
      required:
        [
          submission_id,
          workflow_state,
          evaluation_run,
          question_evaluations,
          automated_evaluation_active,
        ]
      properties:
        submission_id: { type: string, format: uuid }
        workflow_state: { type: string }
        student_match_state: { type: string }
        transcription_state: { type: string }
        assessment_version_id: { type: string, format: uuid }
        evaluation_run:
          anyOf:
            - { $ref: "#/components/schemas/EvaluationRun" }
            - { type: "null" }
        question_evaluations:
          type: array
          items: { $ref: "#/components/schemas/QuestionEvaluation" }
        automated_evaluation_active: { type: boolean }
    OverrideEvaluationInput:
      type: object
      additionalProperties: false
      required: [score, reason]
      properties:
        score: { type: number, minimum: 0 }
        reason: { type: string, minLength: 1, maxLength: 4000 }
        feedback: { type: [string, "null"], maxLength: 5000 }
        valid_alternative: { type: boolean, default: false }
        criterion_finals:
          type: array
          maxItems: 50
          items:
            type: object
            additionalProperties: false
            required: [rubric_criterion_id, final_marks]
            properties:
              rubric_criterion_id: { type: string, format: uuid }
              final_marks: { type: number, minimum: 0 }
    EvaluationOverrideInput:
      $ref: "#/components/schemas/OverrideEvaluationInput"
    EvaluationFeedbackInput:
      $ref: "#/components/schemas/FeedbackEvaluationInput"
    CriterionEvaluation:
      $ref: "#/components/schemas/CriterionDecision"
    ReviewAction:
      type: object
      required:
        [
          id,
          tenant_id,
          submission_id,
          question_evaluation_id,
          evaluation_run_id,
          actor_user_id,
          action_type,
          created_at,
        ]
      properties:
        id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid }
        submission_id: { type: string, format: uuid }
        question_evaluation_id: { type: string, format: uuid }
        evaluation_run_id: { type: string, format: uuid }
        actor_user_id: { type: string, format: uuid }
        action_type:
          type: string
          enum: [ACCEPT, OVERRIDE, EDIT_FEEDBACK, VALID_ALTERNATIVE, ESCALATE]
        reason: { type: [string, "null"] }
        before_snapshot: { type: object }
        after_snapshot: { type: object }
        previous_score: { type: [number, "null"] }
        new_score: { type: [number, "null"] }
        correlation_id: { type: [string, "null"] }
        created_at: { type: string, format: date-time }
    FeedbackEvaluationInput:
      type: object
      additionalProperties: false
      required: [feedback]
      properties:
        feedback: { type: string, minLength: 1, maxLength: 4000 }
    EscalateEvaluationInput:
      type: object
      additionalProperties: false
      required: [reason]
      properties:
        reason: { type: string, minLength: 1, maxLength: 4000 }
    PublicationPrepareResult:
      type: object
      required: [submission_id, workflow_state, published_result_id, status, version_number]
      properties:
        submission_id: { type: string, format: uuid }
        workflow_state: { type: string }
        published_result_id: { type: string, format: uuid }
        status: { type: string, enum: [READY, GENERATING, GENERATED, PUBLISHED, FAILED] }
        version_number: { type: integer }
        job_id: { type: [string, "null"], format: uuid }
    PublicationRegenerateResult:
      type: object
      required: [published_result_id, status, version_number, job_id]
      properties:
        published_result_id: { type: string, format: uuid }
        status: { type: string }
        version_number: { type: integer }
        job_id: { type: string, format: uuid }
        supersedes_result_id: { type: [string, "null"], format: uuid }
    PublicationPublishResult:
      type: object
      required: [published_result_id, status, submission_id]
      properties:
        published_result_id: { type: string, format: uuid }
        status: { type: string, enum: [PUBLISHED] }
        submission_id: { type: string, format: uuid }
        published_at: { type: [string, "null"], format: date-time }
    PublishedResultArtifactInfo:
      type: object
      properties:
        available: { type: boolean }
        sha256: { type: [string, "null"] }
        byte_size: { type: [integer, "null"] }
    PublishedResultSummary:
      type: object
      required: [id, submission_id, status, version_number, ledger_snapshot_hash]
      properties:
        id: { type: string, format: uuid }
        submission_id: { type: string, format: uuid }
        student_id: { type: [string, "null"], format: uuid }
        assessment_id: { type: string, format: uuid }
        assessment_version_id: { type: string, format: uuid }
        evaluation_run_id: { type: string, format: uuid }
        version_number: { type: integer }
        status: { type: string, enum: [READY, GENERATING, GENERATED, PUBLISHED, FAILED] }
        ledger_snapshot_hash: { type: string }
        total_score: { type: number }
        max_total_score: { type: number }
        narrative_source: { type: [string, "null"], enum: [AI, FIXED, RULES_FALLBACK, null] }
        generated_at: { type: [string, "null"], format: date-time }
        published_at: { type: [string, "null"], format: date-time }
        failure_code: { type: [string, "null"] }
        failure_detail: { type: [string, "null"] }
        artifacts:
          type: object
          additionalProperties: { $ref: "#/components/schemas/PublishedResultArtifactInfo" }
    PublicationAnnotation:
      type: object
      required: [id, annotation_type, submission_page_id, x, y, width, height, source_type]
      properties:
        id: { type: string, format: uuid }
        annotation_type:
          type: string
          enum: [TICK, CROSS, PARTIAL, MARK, COMMENT, HIGHLIGHT]
        submission_page_id: { type: string, format: uuid }
        question_evaluation_id: { type: [string, "null"], format: uuid }
        answer_region_id: { type: [string, "null"], format: uuid }
        x: { type: number }
        y: { type: number }
        width: { type: number }
        height: { type: number }
        payload: { type: object }
        source_type: { type: string, enum: [LEDGER, HUMAN] }
    PublicationWorkspace:
      type: object
      required: [submission_id, workflow_state, versions, annotations]
      properties:
        submission_id: { type: string, format: uuid }
        workflow_state: { type: string }
        latest: { oneOf: [{ $ref: "#/components/schemas/PublishedResultSummary" }, { type: "null" }] }
        versions:
          type: array
          items: { $ref: "#/components/schemas/PublishedResultSummary" }
        annotations:
          type: array
          items: { $ref: "#/components/schemas/PublicationAnnotation" }
    ManualAnnotationInput:
      type: object
      additionalProperties: false
      required: [annotation_type, submission_page_id, x, y, width, height]
      properties:
        annotation_type: { type: string, enum: [COMMENT, HIGHLIGHT] }
        submission_page_id: { type: string, format: uuid }
        x: { type: number, minimum: 0, maximum: 1 }
        y: { type: number, minimum: 0, maximum: 1 }
        width: { type: number, exclusiveMinimum: 0, maximum: 1 }
        height: { type: number, exclusiveMinimum: 0, maximum: 1 }
        payload: { type: object }
    PublicationReportPayload:
      description: Discriminated by audience; mirrors student/parent/teacher report schemas
      type: object
      additionalProperties: true
    StudentReport:
      type: object
      description: See packages/contracts/schemas/student-report.schema.json
      additionalProperties: true
    ParentReport:
      type: object
      description: See packages/contracts/schemas/parent-report.schema.json
      additionalProperties: true
    TeacherReport:
      type: object
      description: See packages/contracts/schemas/teacher-report.schema.json
      additionalProperties: true
    AssessmentAnalytics:
      type: object
      description: See packages/contracts/schemas/assessment-analytics.schema.json
      required:
        [
          assessment,
          published_attempt_count,
          unique_student_count,
          mean_percentage,
          median_percentage,
          pass_threshold_percent,
          pass_rate,
          score_distribution,
          question_performance,
          error_distribution,
          curriculum_performance,
          mapped_scorable_question_count,
          unmapped_scorable_question_count,
          mastery_coverage_ratio,
          source,
          as_of,
        ]
      properties:
        assessment:
          type: object
          required: [id, code, title, curriculum_id, status]
          properties:
            id: { type: string, format: uuid }
            code: { type: string }
            title: { type: string }
            curriculum_id: { type: string, format: uuid }
            status: { type: string }
        class_section_id: { type: [string, "null"], format: uuid }
        published_attempt_count: { type: integer, minimum: 0 }
        unique_student_count: { type: integer, minimum: 0 }
        mean_percentage: { type: [number, "null"] }
        median_percentage: { type: [number, "null"] }
        pass_threshold_percent: { type: [number, "null"] }
        pass_rate: { type: [number, "null"] }
        score_distribution:
          type: array
          items:
            type: object
            required: [lower_bound, upper_bound, count]
            properties:
              lower_bound: { type: number }
              upper_bound: { type: number }
              count: { type: integer }
        question_performance:
          type: array
          items: { type: object, additionalProperties: true }
        error_distribution:
          type: object
          required: [academic, review_conditions]
          properties:
            academic:
              type: array
              items: { type: object, additionalProperties: true }
            review_conditions:
              type: array
              items: { type: object, additionalProperties: true }
        curriculum_performance:
          type: array
          items: { type: object, additionalProperties: true }
        mapped_scorable_question_count: { type: integer }
        unmapped_scorable_question_count: { type: integer }
        mastery_coverage_ratio: { type: [number, "null"] }
        source: { type: string, enum: [PUBLISHED_LEDGER] }
        as_of: { type: string, format: date-time }
    StudentAnalytics:
      type: object
      description: See packages/contracts/schemas/student-analytics.schema.json
      required:
        [
          student,
          published_assessment_count,
          published_attempt_count,
          average_percentage,
          concept_signals,
          error_distribution,
          mastery_coverage,
          materialization_status,
          published_result_count,
          materialized_result_count,
          source,
          as_of,
        ]
      properties:
        student:
          type: object
          required: [id, display_name]
          properties:
            id: { type: string, format: uuid }
            display_name: { type: [string, "null"] }
            student_code: { type: [string, "null"] }
            external_ref: { type: [string, "null"] }
        published_assessment_count: { type: integer }
        published_attempt_count: { type: integer }
        average_percentage: { type: [number, "null"] }
        concept_signals:
          type: array
          items:
            type: object
            required: [curriculum_node_id, concept, execution, procedure]
            properties:
              curriculum_node_id: { type: string, format: uuid }
              code: { type: string }
              title: { type: string }
              node_type: { type: string }
              concept: { $ref: "#/components/schemas/MasterySignalBucket" }
              execution: { $ref: "#/components/schemas/MasterySignalBucket" }
              procedure: { $ref: "#/components/schemas/MasterySignalBucket" }
              evidence_count: { type: integer }
              mean_score_ratio: { type: [number, "null"] }
        error_distribution:
          type: array
          items:
            type: object
            properties:
              code: { type: string }
              count: { type: integer }
        mastery_coverage:
          type: object
          properties:
            curriculum_node_count: { type: integer }
            evidence_row_count: { type: integer }
        materialization_status:
          type: string
          enum: [NOT_STARTED, QUEUED, RUNNING, READY, PARTIAL, FAILED]
        published_result_count: { type: integer }
        materialized_result_count: { type: integer }
        source: { type: string, enum: [PUBLISHED_LEDGER] }
        as_of: { type: string, format: date-time }
    MasterySignalBucket:
      type: object
      required: [strong_count, weak_count, inconclusive_count, signal]
      properties:
        strong_count: { type: integer }
        weak_count: { type: integer }
        inconclusive_count: { type: integer }
        signal: { type: string, enum: [STRONG, WEAK, INCONCLUSIVE] }
    MasteryEvidenceList:
      type: object
      description: See packages/contracts/schemas/mastery-evidence.schema.json
      required: [student_id, items]
      properties:
        student_id: { type: string, format: uuid }
        items:
          type: array
          items: { $ref: "#/components/schemas/MasteryEvidenceItem" }
    MasteryEvidenceItem:
      type: object
      required:
        [
          id,
          published_result_id,
          assessment_id,
          assessment_version_id,
          submission_id,
          evaluation_run_id,
          question_evaluation_id,
          question_version_id,
          curriculum_id,
          curriculum_node_id,
          evidence_type,
          strength,
          score_ratio,
          source_final_score,
          source_max_mark,
          mapping_types,
          academic_error_codes,
          review_condition_codes,
          reason_codes,
          source_ledger_snapshot_hash,
          algorithm_version,
          created_at,
        ]
      properties:
        id: { type: string, format: uuid }
        published_result_id: { type: string, format: uuid }
        assessment_id: { type: string, format: uuid }
        assessment_version_id: { type: string, format: uuid }
        submission_id: { type: string, format: uuid }
        evaluation_run_id: { type: string, format: uuid }
        question_evaluation_id: { type: string, format: uuid }
        question_version_id: { type: string, format: uuid }
        curriculum_id: { type: string, format: uuid }
        curriculum_node_id: { type: string, format: uuid }
        evidence_type: { type: string, enum: [CONCEPT, EXECUTION, PROCEDURE] }
        strength: { type: string, enum: [STRONG, WEAK, INCONCLUSIVE] }
        score_ratio: { type: number }
        source_final_score: { type: number }
        source_max_mark: { type: number }
        mapping_types:
          type: array
          items: { type: string }
        mapping_weight: { type: [number, "null"] }
        academic_error_codes:
          type: array
          items: { type: string }
        review_condition_codes:
          type: array
          items: { type: string }
        reason_codes:
          type: array
          items: { type: string }
        source_ledger_snapshot_hash: { type: string }
        algorithm_version: { type: string, enum: [B8_V1] }
        created_at: { type: string, format: date-time }
    AnalyticsPrepareResult:
      type: object
      required:
        [
          published_result_id,
          pipeline_job_id,
          job_status,
          algorithm_version,
        ]
      properties:
        published_result_id: { type: string, format: uuid }
        pipeline_job_id: { type: string, format: uuid }
        job_status: { type: string }
        celery_task_id: { type: [string, "null"] }
        enqueue_error: { type: [string, "null"] }
        algorithm_version: { type: string, enum: [B8_V1] }
    StudentMasteryState:
      type: object
      description: See packages/contracts/schemas/mastery-state.schema.json
      required: [student_id, items, algorithm_version, source, as_of]
      properties:
        student_id: { type: string, format: uuid }
        items:
          type: array
          items: { $ref: "#/components/schemas/MasteryStateItem" }
        algorithm_version: { type: string, enum: [B12_V1] }
        source: { type: string, enum: [MASTERY_EVIDENCE] }
        as_of: { type: string, format: date-time }
    MasteryStateItem:
      type: object
      required:
        [
          curriculum_node_id,
          curriculum_id,
          code,
          title,
          node_type,
          concept_mastery,
          execution_accuracy,
          concept_decisive_count,
          execution_decisive_count,
          concept_inconclusive_count,
          execution_inconclusive_count,
          evidence_count,
          insufficient_concept_evidence,
          insufficient_execution_evidence,
          source_evidence_hash,
          algorithm_version,
          last_updated_at,
        ]
      properties:
        id: { type: [string, "null"], format: uuid }
        curriculum_node_id: { type: string, format: uuid }
        curriculum_id: { type: string, format: uuid }
        code: { type: string }
        title: { type: string }
        node_type: { type: string }
        concept_mastery: { type: [number, "null"], minimum: 0, maximum: 1 }
        execution_accuracy: { type: [number, "null"], minimum: 0, maximum: 1 }
        concept_decisive_count: { type: integer, minimum: 0 }
        execution_decisive_count: { type: integer, minimum: 0 }
        concept_inconclusive_count: { type: integer, minimum: 0 }
        execution_inconclusive_count: { type: integer, minimum: 0 }
        evidence_count: { type: integer, minimum: 0 }
        insufficient_concept_evidence: { type: boolean }
        insufficient_execution_evidence: { type: boolean }
        source_evidence_hash: { type: string }
        algorithm_version: { type: string, enum: [B12_V1] }
        last_updated_at: { type: string, format: date-time }
    StudentMasteryTrend:
      type: object
      description: See packages/contracts/schemas/mastery-trend.schema.json
      required: [student_id, points, algorithm_version, source, as_of]
      properties:
        student_id: { type: string, format: uuid }
        curriculum_node_id: { type: [string, "null"], format: uuid }
        points:
          type: array
          items: { $ref: "#/components/schemas/MasteryTrendPoint" }
        algorithm_version: { type: string, enum: [B12_V1] }
        source: { type: string, enum: [MASTERY_EVIDENCE] }
        as_of: { type: string, format: date-time }
    MasteryTrendPoint:
      type: object
      required:
        [
          curriculum_node_id,
          curriculum_id,
          code,
          title,
          published_result_id,
          assessment_id,
          assessment_code,
          effective_at,
          concept_mastery,
          execution_accuracy,
          concept_decisive_count,
          execution_decisive_count,
          evidence_count,
          insufficient_concept_evidence,
          insufficient_execution_evidence,
          source_evidence_hash,
          algorithm_version,
        ]
      properties:
        curriculum_node_id: { type: string, format: uuid }
        curriculum_id: { type: string, format: uuid }
        code: { type: string }
        title: { type: string }
        published_result_id: { type: string, format: uuid }
        assessment_id: { type: string, format: uuid }
        assessment_code: { type: string }
        effective_at: { type: string, format: date-time }
        concept_mastery: { type: [number, "null"], minimum: 0, maximum: 1 }
        execution_accuracy: { type: [number, "null"], minimum: 0, maximum: 1 }
        concept_decisive_count: { type: integer, minimum: 0 }
        execution_decisive_count: { type: integer, minimum: 0 }
        evidence_count: { type: integer, minimum: 0 }
        insufficient_concept_evidence: { type: boolean }
        insufficient_execution_evidence: { type: boolean }
        source_evidence_hash: { type: string }
        algorithm_version: { type: string, enum: [B12_V1] }
    StudentRepeatedErrors:
      type: object
      description: See packages/contracts/schemas/repeated-errors.schema.json
      required:
        [student_id, items, recurrence_threshold, algorithm_version, source, as_of]
      properties:
        student_id: { type: string, format: uuid }
        items:
          type: array
          items: { $ref: "#/components/schemas/RepeatedErrorItem" }
        recurrence_threshold: { type: integer, enum: [2] }
        algorithm_version: { type: string, enum: [B12_V1] }
        source: { type: string, enum: [MASTERY_EVIDENCE] }
        as_of: { type: string, format: date-time }
    RepeatedErrorItem:
      type: object
      required:
        [
          error_code,
          occurrence_count,
          distinct_published_result_count,
          distinct_assessment_count,
          first_seen_at,
          last_seen_at,
          affected_question_evaluations,
          curriculum_node_ids,
        ]
      properties:
        error_code: { type: string }
        occurrence_count: { type: integer, minimum: 1 }
        distinct_published_result_count: { type: integer, minimum: 2 }
        distinct_assessment_count: { type: integer, minimum: 1 }
        first_seen_at: { type: string, format: date-time }
        last_seen_at: { type: string, format: date-time }
        affected_question_evaluations:
          type: array
          items:
            type: object
            required:
              [
                published_result_id,
                assessment_id,
                question_evaluation_id,
                question_version_id,
              ]
            properties:
              published_result_id: { type: string, format: uuid }
              assessment_id: { type: string, format: uuid }
              question_evaluation_id: { type: string, format: uuid }
              question_version_id: { type: string, format: uuid }
        curriculum_node_ids:
          type: array
          items: { type: string, format: uuid }
    StudentRecoverableMarks:
      type: object
      description: See packages/contracts/schemas/recoverable-marks.schema.json
      required:
        [
          student_id,
          total_lost_marks,
          attributed_potentially_recoverable_marks,
          unattributed_lost_marks,
          items,
          disclaimer,
          algorithm_version,
          source,
          as_of,
        ]
      properties:
        student_id: { type: string, format: uuid }
        total_lost_marks: { type: string }
        attributed_potentially_recoverable_marks: { type: string }
        unattributed_lost_marks: { type: string }
        items:
          type: array
          items: { $ref: "#/components/schemas/RecoverableMarksItem" }
        disclaimer:
          type: string
          enum:
            - "Potentially recoverable marks are an analytical estimate from final criterion deductions, not guaranteed recovery."
        algorithm_version: { type: string, enum: [B12_V1] }
        source: { type: string, enum: [PUBLISHED_LEDGER] }
        as_of: { type: string, format: date-time }
    RecoverableMarksItem:
      type: object
      required:
        [
          error_code,
          potentially_recoverable_marks,
          occurrence_count,
          percent_of_total_lost,
          affected_references,
        ]
      properties:
        error_code: { type: string }
        potentially_recoverable_marks: { type: string }
        occurrence_count: { type: integer, minimum: 1 }
        percent_of_total_lost: { type: [number, "null"], minimum: 0, maximum: 100 }
        affected_references:
          type: array
          items:
            type: object
            required:
              [
                published_result_id,
                assessment_id,
                question_evaluation_id,
                criterion_evaluation_id,
              ]
            properties:
              published_result_id: { type: string, format: uuid }
              assessment_id: { type: string, format: uuid }
              question_evaluation_id: { type: string, format: uuid }
              criterion_evaluation_id: { type: string, format: uuid }
    StudentMistakeNotebook:
      type: object
      description: See packages/contracts/schemas/mistake-notebook.schema.json
      required: [student_id, entries, algorithm_version, source, as_of]
      properties:
        student_id: { type: string, format: uuid }
        entries:
          type: array
          items: { $ref: "#/components/schemas/MistakeNotebookEntry" }
        algorithm_version: { type: string, enum: [B12_V1] }
        source: { type: string, enum: [PUBLISHED_LEDGER] }
        as_of: { type: string, format: date-time }
    MistakeNotebookEntry:
      type: object
      required:
        [
          id,
          published_result_id,
          assessment_id,
          assessment_code,
          submission_id,
          question_evaluation_id,
          question_version_id,
          question_code,
          academic_error_code,
          final_score,
          max_mark,
          deduction_reasons,
          first_divergence_step,
          curriculum_nodes,
          recommended_practice_kind,
          linked_learning_recommendation_ids,
          source_ledger_snapshot_hash,
          algorithm_version,
          materialized_at,
          effective_at,
        ]
      properties:
        id: { type: string, format: uuid }
        published_result_id: { type: string, format: uuid }
        assessment_id: { type: string, format: uuid }
        assessment_code: { type: string }
        submission_id: { type: string, format: uuid }
        question_evaluation_id: { type: string, format: uuid }
        question_version_id: { type: string, format: uuid }
        question_code: { type: string }
        academic_error_code: { type: string }
        final_score: { type: string }
        max_mark: { type: string }
        deduction_reasons:
          type: array
          items: { type: string }
        first_divergence_step: { type: [string, "null"] }
        curriculum_nodes:
          type: array
          items:
            type: object
            required: [id, code, title, node_type]
            properties:
              id: { type: string, format: uuid }
              code: { type: string }
              title: { type: string }
              node_type: { type: string }
        recommended_practice_kind:
          type: string
          enum: [CONCEPT_CHECK, EXECUTION_PRACTICE, PROCEDURE_PRACTICE]
        linked_learning_recommendation_ids:
          type: array
          items: { type: string, format: uuid }
        source_ledger_snapshot_hash: { type: string }
        algorithm_version: { type: string, enum: [B12_V1] }
        materialized_at: { type: string, format: date-time }
        effective_at: { type: string, format: date-time }
    B12RebuildResult:
      type: object
      description: See packages/contracts/schemas/b12-rebuild-result.schema.json
      required:
        [
          student_id,
          algorithm_version,
          mastery_state_count,
          snapshot_count,
          notebook_entry_count,
          source_evidence_hash,
          source,
        ]
      properties:
        student_id: { type: string, format: uuid }
        algorithm_version: { type: string, enum: [B12_V1] }
        mastery_state_count: { type: integer, minimum: 0 }
        snapshot_count: { type: integer, minimum: 0 }
        notebook_entry_count: { type: integer, minimum: 0 }
        source_evidence_hash: { type: string }
        source: { type: string, enum: [MASTERY_EVIDENCE] }
    LearningWorkspace:
      type: object
      description: Live B9 learning workspace
      required:
        [
          student,
          available_curricula,
          selected_curriculum_id,
          materialization_status,
          evidence_coverage,
          is_stale,
        ]
      properties:
        student:
          type: object
          required: [id, display_name]
          properties:
            id: { type: string, format: uuid }
            display_name: { type: string }
        available_curricula:
          type: array
          items:
            type: object
            required: [id, code, name]
            properties:
              id: { type: string, format: uuid }
              curriculum_id: { type: string, format: uuid }
              code: { type: string }
              name: { type: string }
              evidence_count: { type: integer }
        selected_curriculum_id: { type: [string, "null"], format: uuid }
        selected_curriculum:
          type: [object, "null"]
          properties:
            id: { type: string, format: uuid }
            code: { type: string }
            name: { type: string }
        materialization_status: { type: string }
        materialization: { type: object, additionalProperties: true }
        evidence_coverage:
          type: object
          properties:
            evidence_count: { type: integer }
            node_count: { type: integer }
            evidence_row_count: { type: integer }
            curriculum_node_count: { type: integer }
        latest_run: { type: [object, "null"], additionalProperties: true }
        latest_plan: { type: [object, "null"], additionalProperties: true }
        is_stale: { type: boolean }
        latest_improvement_blueprint:
          type: [object, "null"]
          additionalProperties: true
        selection_required: { type: boolean }
        resource_assignments:
          type: array
          description: B13 assigned catalog resources for the selected curriculum (ASSIGNED only)
          items: { $ref: "#/components/schemas/StudentResourceAssignment" }
        reassessments:
          type: array
          description: B14 reassessment history for the student (newest first)
          items: { $ref: "#/components/schemas/Reassessment" }
    ReassessmentInstantiateRequest:
      type: object
      description: See packages/contracts/schemas/reassessment-instantiate.schema.json
      required: [items]
      properties:
        items:
          type: array
          minItems: 1
          items:
            type: object
            required: [improvement_assessment_item_id, prompt_text, max_marks]
            properties:
              improvement_assessment_item_id: { type: string, format: uuid }
              prompt_text: { type: string, minLength: 1 }
              max_marks: { type: string }
              question_type: { type: [string, "null"] }
              instructions: { type: [string, "null"] }
    Reassessment:
      type: object
      description: See packages/contracts/schemas/reassessment.schema.json
      required:
        [
          id,
          improvement_assessment_id,
          blueprint_title,
          blueprint_version_number,
          student_id,
          curriculum_id,
          assessment_id,
          assessment_status,
          assessment_version_id,
          submission_id,
          published_result_id,
          status,
          algorithm_version,
          instantiation_hash,
          baseline_captured_at,
          created_at,
          updated_at,
          items,
          mastery_deltas,
        ]
      properties:
        id: { type: string, format: uuid }
        improvement_assessment_id: { type: string, format: uuid }
        blueprint_title: { type: string }
        blueprint_version_number: { type: integer }
        student_id: { type: string, format: uuid }
        curriculum_id: { type: string, format: uuid }
        assessment_id: { type: string, format: uuid }
        assessment_status: { type: string }
        assessment_version_id: { type: string, format: uuid }
        submission_id: { type: [string, "null"], format: uuid }
        published_result_id: { type: [string, "null"], format: uuid }
        status: { type: string, enum: [CREATED, SUBMITTED, PUBLISHED] }
        algorithm_version: { type: string, enum: [B14_V1] }
        instantiation_hash: { type: string }
        baseline_captured_at: { type: string, format: date-time }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
        items:
          type: array
          items: { $ref: "#/components/schemas/ReassessmentItem" }
        mastery_deltas:
          type: array
          items: { $ref: "#/components/schemas/ReassessmentMasteryDelta" }
    ReassessmentItem:
      type: object
      required:
        [
          id,
          improvement_assessment_item_id,
          question_version_id,
          curriculum_node_id,
          item_code_snapshot,
          template_kind_snapshot,
          question_template_ref_snapshot,
        ]
      properties:
        id: { type: string, format: uuid }
        improvement_assessment_item_id: { type: string, format: uuid }
        question_version_id: { type: string, format: uuid }
        curriculum_node_id: { type: string, format: uuid }
        item_code_snapshot: { type: string }
        template_kind_snapshot: { type: string }
        question_template_ref_snapshot: { type: [string, "null"] }
    ReassessmentMasteryDelta:
      type: object
      required:
        [
          curriculum_node_id,
          baseline_concept_mastery,
          baseline_execution_accuracy,
          baseline_concept_decisive_count,
          baseline_execution_decisive_count,
          baseline_concept_inconclusive_count,
          baseline_execution_inconclusive_count,
          baseline_evidence_count,
          baseline_source_evidence_hash,
          post_snapshot_id,
          post_published_result_id,
          post_concept_mastery,
          post_execution_accuracy,
          post_concept_decisive_count,
          post_execution_decisive_count,
          post_concept_inconclusive_count,
          post_execution_inconclusive_count,
          post_evidence_count,
          post_source_evidence_hash,
          concept_delta,
          execution_delta,
          materialized_at,
          algorithm_version,
        ]
      properties:
        curriculum_node_id: { type: string, format: uuid }
        baseline_concept_mastery: { type: [number, "null"], minimum: 0, maximum: 1 }
        baseline_execution_accuracy: { type: [number, "null"], minimum: 0, maximum: 1 }
        baseline_concept_decisive_count: { type: integer, minimum: 0 }
        baseline_execution_decisive_count: { type: integer, minimum: 0 }
        baseline_concept_inconclusive_count: { type: integer, minimum: 0 }
        baseline_execution_inconclusive_count: { type: integer, minimum: 0 }
        baseline_evidence_count: { type: integer, minimum: 0 }
        baseline_source_evidence_hash: { type: string }
        post_snapshot_id: { type: [string, "null"], format: uuid }
        post_published_result_id: { type: [string, "null"], format: uuid }
        post_concept_mastery: { type: [number, "null"], minimum: 0, maximum: 1 }
        post_execution_accuracy: { type: [number, "null"], minimum: 0, maximum: 1 }
        post_concept_decisive_count: { type: [integer, "null"], minimum: 0 }
        post_execution_decisive_count: { type: [integer, "null"], minimum: 0 }
        post_concept_inconclusive_count: { type: [integer, "null"], minimum: 0 }
        post_execution_inconclusive_count: { type: [integer, "null"], minimum: 0 }
        post_evidence_count: { type: [integer, "null"], minimum: 0 }
        post_source_evidence_hash: { type: [string, "null"] }
        concept_delta: { type: [number, "null"] }
        execution_delta: { type: [number, "null"] }
        materialized_at: { type: [string, "null"], format: date-time }
        algorithm_version: { type: string, enum: [B14_V1] }
    B14RebuildResult:
      type: object
      description: See packages/contracts/schemas/b14-rebuild-result.schema.json
      required: [reassessment_id, algorithm_version, delta_count, published_result_id, source]
      properties:
        reassessment_id: { type: string, format: uuid }
        algorithm_version: { type: string, enum: [B14_V1] }
        delta_count: { type: integer, minimum: 0 }
        published_result_id: { type: [string, "null"], format: uuid }
        source: { type: string, enum: [MASTERY_STATE_SNAPSHOT] }
    CurriculumResource:
      type: object
      description: See packages/contracts/schemas/curriculum-resource.schema.json
      required:
        [
          id,
          curriculum_id,
          code,
          title,
          description,
          resource_kind,
          status,
          content_ref,
          curriculum_node_ids,
          created_by,
          approved_by,
          approved_at,
          created_at,
          updated_at,
        ]
      properties:
        id: { type: string, format: uuid }
        curriculum_id: { type: string, format: uuid }
        code: { type: string }
        title: { type: string }
        description: { type: [string, "null"] }
        resource_kind:
          type: string
          enum: [PRACTICE_SET, WORKED_EXAMPLE, CONCEPT_NOTE, INTERNAL_PACKET]
        status:
          type: string
          enum: [DRAFT, APPROVED, ACTIVE, DEACTIVATED]
        content_ref: { type: string }
        curriculum_node_ids:
          type: array
          items: { type: string, format: uuid }
        created_by: { type: [string, "null"], format: uuid }
        approved_by: { type: [string, "null"], format: uuid }
        approved_at: { type: [string, "null"], format: date-time }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    CurriculumResourceList:
      type: object
      required: [items]
      properties:
        curriculum_id: { type: [string, "null"], format: uuid }
        status_filter:
          type: [string, "null"]
          enum: [DRAFT, APPROVED, ACTIVE, DEACTIVATED, null]
        items:
          type: array
          items: { $ref: "#/components/schemas/CurriculumResource" }
    CurriculumResourceCreate:
      type: object
      required: [curriculum_id, code, title, resource_kind, content_ref, curriculum_node_ids]
      properties:
        curriculum_id: { type: string, format: uuid }
        code: { type: string }
        title: { type: string }
        description: { type: [string, "null"] }
        resource_kind:
          type: string
          enum: [PRACTICE_SET, WORKED_EXAMPLE, CONCEPT_NOTE, INTERNAL_PACKET]
        content_ref: { type: string }
        curriculum_node_ids:
          type: array
          minItems: 1
          items: { type: string, format: uuid }
    CurriculumResourceUpdate:
      type: object
      properties:
        title: { type: string }
        description: { type: [string, "null"] }
        resource_kind:
          type: string
          enum: [PRACTICE_SET, WORKED_EXAMPLE, CONCEPT_NOTE, INTERNAL_PACKET]
        content_ref: { type: string }
    CurriculumResourceNodesReplace:
      type: object
      required: [curriculum_node_ids]
      properties:
        curriculum_node_ids:
          type: array
          minItems: 1
          items: { type: string, format: uuid }
    StudentResourceAssignment:
      type: object
      description: See packages/contracts/schemas/student-resource-assignment.schema.json
      required:
        [
          id,
          student_id,
          resource_id,
          resource,
          learning_recommendation_id,
          status,
          assigned_by,
          assigned_at,
          cancelled_at,
          cancelled_by,
        ]
      properties:
        id: { type: string, format: uuid }
        student_id: { type: string, format: uuid }
        resource_id: { type: string, format: uuid }
        resource: { $ref: "#/components/schemas/CurriculumResource" }
        learning_recommendation_id: { type: [string, "null"], format: uuid }
        status: { type: string, enum: [ASSIGNED, CANCELLED] }
        assigned_by: { type: [string, "null"], format: uuid }
        assigned_at: { type: string, format: date-time }
        cancelled_at: { type: [string, "null"], format: date-time }
        cancelled_by: { type: [string, "null"], format: uuid }
    StudentResourceAssignmentList:
      type: object
      required: [student_id, items]
      properties:
        student_id: { type: string, format: uuid }
        curriculum_id: { type: [string, "null"], format: uuid }
        items:
          type: array
          items: { $ref: "#/components/schemas/StudentResourceAssignment" }
    StudentResourceAssignmentCreate:
      type: object
      required: [resource_id]
      properties:
        resource_id: { type: string, format: uuid }
        learning_recommendation_id: { type: [string, "null"], format: uuid }
    LearningPrepareInput:
      type: object
      required: [curriculum_id]
      properties:
        curriculum_id: { type: string, format: uuid }
    LearningPrepareResult:
      type: object
      required: [run_id, student_id, curriculum_id, version_number, status, algorithm_version]
      properties:
        run_id: { type: string, format: uuid }
        student_id: { type: string, format: uuid }
        curriculum_id: { type: string, format: uuid }
        version_number: { type: integer }
        status: { type: string, enum: [QUEUED, RUNNING, READY, FAILED, SUPERSEDED] }
        input_hash: { type: string }
        algorithm_version: { type: string, enum: [B9_V1] }
        celery_task_id: { type: [string, "null"] }
        enqueue_error: { type: [string, "null"] }
    LearningRecommendationPrerequisite:
      type: object
      required: [curriculum_node_id, relationship_type, sequence]
      properties:
        curriculum_node_id: { type: string, format: uuid }
        relationship_type: { type: string, enum: [REQUIRED, RECOMMENDED] }
        sequence: { type: integer }
    LearningRecommendation:
      type: object
      description: See packages/contracts/schemas/learning-plan.schema.json
      required:
        [
          id,
          target_node_id,
          recommendation_kind,
          priority,
          rationale,
          concept_signal,
          execution_signal,
          procedure_signal,
          evidence_count,
          status,
          prerequisites,
        ]
      properties:
        id: { type: string, format: uuid }
        target_node_id: { type: string, format: uuid }
        recommendation_kind:
          type: string
          enum: [PREREQUISITE_REPAIR, TARGET_CONCEPT, PROCEDURE_PRACTICE, EXECUTION_PRACTICE]
        priority: { type: integer, enum: [1, 2, 3] }
        rationale: { type: string }
        concept_signal: { type: string, enum: [STRONG, WEAK, INCONCLUSIVE] }
        execution_signal: { type: string, enum: [STRONG, WEAK, INCONCLUSIVE] }
        procedure_signal: { type: string, enum: [STRONG, WEAK, INCONCLUSIVE] }
        evidence_count: { type: integer }
        mean_evidence_score_ratio: { type: [number, "null"] }
        status: { type: string, enum: [ACTIVE, DISMISSED, COMPLETED] }
        target_node_code: { type: string }
        target_node_title: { type: string }
        target_node_type: { type: string }
        prerequisites:
          type: array
          items: { $ref: "#/components/schemas/LearningRecommendationPrerequisite" }
        mastery_evidence_ids:
          type: array
          items: { type: string, format: uuid }
    LearningPathStep:
      type: object
      required: [curriculum_node_id, kind, sequence, title, evidence_basis]
      properties:
        id: { type: string, format: uuid }
        curriculum_node_id: { type: string, format: uuid }
        learning_recommendation_id: { type: [string, "null"], format: uuid }
        kind:
          type: string
          enum: [PREREQUISITE, LEARN, GUIDED, INDEPENDENT, MASTERY_CHECK]
        sequence: { type: integer }
        title: { type: string }
        description: { type: [string, "null"] }
        evidence_basis: { type: string }
        relationship_type: { type: [string, "null"], enum: [REQUIRED, RECOMMENDED, null] }
    LearningPlanRun:
      type: object
      description: See packages/contracts/schemas/learning-plan.schema.json
      required:
        [
          id,
          student_id,
          curriculum_id,
          version_number,
          status,
          algorithm_version,
          source_evidence_hash,
          curriculum_graph_hash,
          input_hash,
          is_stale,
          recommendations,
          learning_path,
        ]
      properties:
        id: { type: string, format: uuid }
        student_id: { type: string, format: uuid }
        curriculum_id: { type: string, format: uuid }
        version_number: { type: integer }
        status: { type: string, enum: [QUEUED, RUNNING, READY, FAILED, SUPERSEDED] }
        algorithm_version: { type: string, enum: [B9_V1] }
        generation_source: { type: [string, "null"], enum: [AI, FIXED, RULES_FALLBACK, null] }
        provider: { type: [string, "null"] }
        model: { type: [string, "null"] }
        source_evidence_hash: { type: string }
        curriculum_graph_hash: { type: string }
        input_hash: { type: string }
        is_stale: { type: boolean }
        failure_code: { type: [string, "null"] }
        failure_detail: { type: [string, "null"] }
        requested_at: { type: [string, "null"], format: date-time }
        started_at: { type: [string, "null"], format: date-time }
        finished_at: { type: [string, "null"], format: date-time }
        recommendations:
          type: array
          items: { $ref: "#/components/schemas/LearningRecommendation" }
        learning_path:
          type: array
          items: { $ref: "#/components/schemas/LearningPathStep" }
        no_gap_message: { type: [string, "null"] }
    ImprovementAssessmentItem:
      type: object
      required:
        [
          learning_recommendation_id,
          curriculum_node_id,
          item_code,
          template_kind,
          question_template_ref,
          focus,
          difficulty,
          sort_order,
        ]
      properties:
        id: { type: string, format: uuid }
        learning_recommendation_id: { type: string, format: uuid }
        curriculum_node_id: { type: string, format: uuid }
        item_code: { type: string }
        template_kind:
          type: string
          enum:
            [
              CONCEPT_CHECK,
              PREREQUISITE_CHECK,
              PROCEDURE_PRACTICE,
              EXECUTION_PRACTICE,
              TRANSFER_CHECK,
            ]
        question_template_ref: { type: string }
        focus: { type: string }
        difficulty: { type: string, enum: [EASY, MEDIUM, HARD] }
        suggested_marks: { type: [number, "null"] }
        sort_order: { type: integer }
    ImprovementAssessment:
      type: object
      description: See packages/contracts/schemas/improvement-assessment-blueprint.schema.json
      required:
        [
          id,
          student_id,
          curriculum_id,
          learning_plan_run_id,
          version_number,
          title,
          status,
          source_evidence_hash,
          curriculum_graph_hash,
          input_hash,
          items,
        ]
      properties:
        id: { type: string, format: uuid }
        student_id: { type: string, format: uuid }
        curriculum_id: { type: string, format: uuid }
        learning_plan_run_id: { type: string, format: uuid }
        version_number: { type: integer }
        title: { type: string }
        status:
          type: string
          enum: [DRAFT, GENERATING, PENDING_APPROVAL, APPROVED, REJECTED, FAILED]
        source_evidence_hash: { type: string }
        curriculum_graph_hash: { type: string }
        input_hash: { type: string }
        generation_source: { type: [string, "null"], enum: [AI, FIXED, RULES_FALLBACK, null] }
        provider: { type: [string, "null"] }
        model: { type: [string, "null"] }
        blueprint_storage_key: { type: [string, "null"] }
        blueprint_sha256: { type: [string, "null"] }
        blueprint_byte_size: { type: [integer, "null"] }
        generated_at: { type: [string, "null"], format: date-time }
        approved_at: { type: [string, "null"], format: date-time }
        rejected_at: { type: [string, "null"], format: date-time }
        rejection_reason: { type: [string, "null"] }
        failure_code: { type: [string, "null"] }
        failure_detail: { type: [string, "null"] }
        items:
          type: array
          items: { $ref: "#/components/schemas/ImprovementAssessmentItem" }
    ImprovementBlueprintPrepareResult:
      type: object
      required:
        [
          improvement_assessment_id,
          learning_plan_run_id,
          version_number,
          status,
        ]
      properties:
        improvement_assessment_id: { type: string, format: uuid }
        learning_plan_run_id: { type: string, format: uuid }
        version_number: { type: integer }
        status:
          type: string
          enum: [DRAFT, GENERATING, PENDING_APPROVAL, APPROVED, REJECTED, FAILED]
        input_hash: { type: string }
        celery_task_id: { type: [string, "null"] }
        enqueue_error: { type: [string, "null"] }
    ImprovementBlueprintApproveResult:
      allOf:
        - { $ref: "#/components/schemas/ImprovementAssessment" }
    ImprovementBlueprintRejectInput:
      type: object
      required: [reason]
      properties:
        reason: { type: string, minLength: 1, maxLength: 2000 }
    BenchmarkThresholdProfile:
      type: object
      additionalProperties: false
      required:
        [
          profile_code,
          algorithm_version,
          max_missing_output_rate,
          max_mean_abs_score_error,
          min_exact_score_agreement_rate,
          min_taxonomy_agreement_rate,
          max_safety_invariant_failure_rate,
          score_tolerance,
        ]
      properties:
        profile_code: { type: string, minLength: 1 }
        algorithm_version: { type: string, minLength: 1 }
        max_missing_output_rate: { type: number, minimum: 0, maximum: 1 }
        max_mean_abs_score_error: { type: number, minimum: 0 }
        min_exact_score_agreement_rate: { type: number, minimum: 0, maximum: 1 }
        min_taxonomy_agreement_rate: { type: number, minimum: 0, maximum: 1 }
        max_safety_invariant_failure_rate: { type: number, minimum: 0, maximum: 1 }
        score_tolerance: { type: number, minimum: 0 }
    BenchmarkDataset:
      type: object
      additionalProperties: false
      required: [id, code, title, description, created_by, created_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        code: { type: string }
        title: { type: string }
        description: { type: [string, "null"] }
        created_by: { type: [string, "null"], format: uuid }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    BenchmarkDatasetCreate:
      type: object
      additionalProperties: false
      required: [code, title]
      properties:
        code: { type: string, minLength: 1, maxLength: 100 }
        title: { type: string, minLength: 1, maxLength: 255 }
        description: { type: [string, "null"], maxLength: 4000 }
    BenchmarkDatasetList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/BenchmarkDataset" }
    BenchmarkVersion:
      type: object
      additionalProperties: false
      required:
        [
          id,
          dataset_id,
          version_number,
          status,
          threshold_profile_snapshot,
          case_count,
          content_hash,
          locked_by,
          locked_at,
          created_by,
          created_at,
          updated_at,
        ]
      properties:
        id: { type: string, format: uuid }
        dataset_id: { type: string, format: uuid }
        version_number: { type: integer, minimum: 1 }
        status: { type: string, enum: [DRAFT, LOCKED] }
        threshold_profile_snapshot: { $ref: "#/components/schemas/BenchmarkThresholdProfile" }
        case_count: { type: integer, minimum: 0 }
        content_hash: { type: [string, "null"] }
        locked_by: { type: [string, "null"], format: uuid }
        locked_at: { type: [string, "null"], format: date-time }
        created_by: { type: [string, "null"], format: uuid }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    BenchmarkVersionCreate:
      type: object
      additionalProperties: false
      properties:
        threshold_profile_snapshot:
          oneOf:
            - { $ref: "#/components/schemas/BenchmarkThresholdProfile" }
            - { type: "null" }
    BenchmarkVersionList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/BenchmarkVersion" }
    BenchmarkCase:
      type: object
      additionalProperties: false
      required:
        [
          id,
          dataset_version_id,
          published_result_id,
          evaluation_run_id,
          question_evaluation_id,
          question_version_id,
          rubric_version_id,
          assessment_version_id,
          expected_final_marks,
          expected_max_marks,
          expected_error_codes,
          source_ledger_hash,
          evidence_hash,
          adjudicated_by,
          adjudicated_at,
          replay_fixture,
          created_at,
          updated_at,
        ]
      properties:
        id: { type: string, format: uuid }
        dataset_version_id: { type: string, format: uuid }
        published_result_id: { type: string, format: uuid }
        evaluation_run_id: { type: string, format: uuid }
        question_evaluation_id: { type: string, format: uuid }
        question_version_id: { type: string, format: uuid }
        rubric_version_id: { type: string, format: uuid }
        assessment_version_id: { type: string, format: uuid }
        expected_final_marks: { type: number }
        expected_max_marks: { type: number }
        expected_error_codes:
          type: array
          items: { type: string }
        source_ledger_hash: { type: string }
        evidence_hash: { type: string }
        adjudicated_by: { type: [string, "null"], format: uuid }
        adjudicated_at: { type: string, format: date-time }
        replay_fixture: { type: object }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    BenchmarkCaseCreate:
      type: object
      additionalProperties: false
      required: [published_result_id, question_evaluation_id]
      properties:
        published_result_id: { type: string, format: uuid }
        question_evaluation_id: { type: string, format: uuid }
    BenchmarkCaseList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/BenchmarkCase" }
    BenchmarkEligibleSources:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items:
            type: object
            additionalProperties: false
            required:
              [
                published_result_id,
                evaluation_run_id,
                submission_id,
                assessment_version_id,
                ledger_snapshot_hash,
                question_evaluations,
              ]
            properties:
              published_result_id: { type: string, format: uuid }
              evaluation_run_id: { type: string, format: uuid }
              submission_id: { type: string, format: uuid }
              assessment_version_id: { type: string, format: uuid }
              ledger_snapshot_hash: { type: string }
              question_evaluations:
                type: array
                items:
                  type: object
                  additionalProperties: false
                  required:
                    [
                      question_evaluation_id,
                      question_version_id,
                      workflow_state,
                      final_human_approved_score,
                      max_mark,
                      error_codes,
                    ]
                  properties:
                    question_evaluation_id: { type: string, format: uuid }
                    question_version_id: { type: string, format: uuid }
                    workflow_state: { type: string, enum: [ACCEPTED, OVERRIDDEN] }
                    final_human_approved_score: { type: number }
                    max_mark: { type: number }
                    error_codes:
                      type: array
                      items: { type: string }
    BenchmarkRegressionRun:
      type: object
      additionalProperties: false
      required:
        [
          id,
          dataset_version_id,
          status,
          verdict,
          idempotency_key,
          candidate_provider,
          candidate_model,
          candidate_model_version,
          candidate_prompt_template_version,
          candidate_config,
          threshold_snapshot,
          aggregate_metrics,
          initiated_by,
          started_at,
          finished_at,
          failure_code,
          failure_detail,
          created_at,
          updated_at,
        ]
      properties:
        id: { type: string, format: uuid }
        dataset_version_id: { type: string, format: uuid }
        status: { type: string, enum: [QUEUED, RUNNING, PASSED, FAILED, ERROR] }
        verdict: { type: string, enum: [PASS, FAIL, PENDING] }
        idempotency_key: { type: [string, "null"] }
        candidate_provider: { type: string }
        candidate_model: { type: string }
        candidate_model_version: { type: string }
        candidate_prompt_template_version: { type: string }
        candidate_config: { type: object }
        threshold_snapshot: { $ref: "#/components/schemas/BenchmarkThresholdProfile" }
        aggregate_metrics: { type: object }
        initiated_by: { type: [string, "null"], format: uuid }
        started_at: { type: [string, "null"], format: date-time }
        finished_at: { type: [string, "null"], format: date-time }
        failure_code: { type: [string, "null"] }
        failure_detail: { type: [string, "null"] }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    BenchmarkRegressionRunCreate:
      type: object
      additionalProperties: false
      required:
        [
          candidate_provider,
          candidate_model,
          candidate_model_version,
          candidate_prompt_template_version,
        ]
      properties:
        candidate_provider: { type: string, minLength: 1, maxLength: 100 }
        candidate_model: { type: string, minLength: 1, maxLength: 100 }
        candidate_model_version: { type: string, minLength: 1, maxLength: 100 }
        candidate_prompt_template_version: { type: string, minLength: 1, maxLength: 100 }
        candidate_config: { type: [object, "null"] }
        idempotency_key: { type: [string, "null"], maxLength: 128 }
    BenchmarkRegressionRunList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/BenchmarkRegressionRun" }
    BenchmarkRegressionCaseResult:
      type: object
      additionalProperties: false
      required:
        [
          id,
          regression_run_id,
          benchmark_case_id,
          missing_output,
          actual_marks,
          actual_error_codes,
          score_abs_error,
          exact_score_match,
          taxonomy_match,
          safety_invariant_failed,
          diff,
          ai_execution_record_id,
          created_at,
          updated_at,
        ]
      properties:
        id: { type: string, format: uuid }
        regression_run_id: { type: string, format: uuid }
        benchmark_case_id: { type: string, format: uuid }
        missing_output: { type: boolean }
        actual_marks: { type: [number, "null"] }
        actual_error_codes:
          type: array
          items: { type: string }
        score_abs_error: { type: [number, "null"] }
        exact_score_match: { type: boolean }
        taxonomy_match: { type: [boolean, "null"] }
        safety_invariant_failed: { type: boolean }
        diff: { type: object }
        ai_execution_record_id: { type: [string, "null"], format: uuid }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    BenchmarkRegressionCaseResultList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/BenchmarkRegressionCaseResult" }
    BenchmarkGateVerdict:
      type: object
      additionalProperties: false
      required:
        [
          verdict,
          passed,
          run_id,
          candidate_provider,
          candidate_model,
          candidate_model_version,
          candidate_prompt_template_version,
          metrics,
          threshold_snapshot,
        ]
      properties:
        verdict: { type: string, enum: [PASS, FAIL, PENDING] }
        passed: { type: boolean }
        run_id: { type: string, format: uuid }
        status: { type: string, enum: [QUEUED, RUNNING, PASSED, FAILED, ERROR] }
        candidate_provider: { type: string }
        candidate_model: { type: string }
        candidate_model_version: { type: string }
        candidate_prompt_template_version: { type: string }
        metrics: { type: object }
        threshold_snapshot: { $ref: "#/components/schemas/BenchmarkThresholdProfile" }


    GradingPool:
      type: object
      additionalProperties: false
      required: [id, assessment_id, assessment_version_id, grading_mode, status, allocation_strategy, created_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid }
        assessment_id: { type: string, format: uuid }
        assessment_version_id: { type: string, format: uuid }
        grading_mode: { type: string, enum: [HORIZONTAL_QUESTION] }
        status: { type: string, enum: [DRAFT, ACTIVE, CLOSED] }
        allocation_strategy: { type: string, enum: [MANUAL, ROUND_ROBIN] }
        created_by: { type: [string, "null"], format: uuid }
        activated_by: { type: [string, "null"], format: uuid }
        activated_at: { type: [string, "null"], format: date-time }
        closed_by: { type: [string, "null"], format: uuid }
        closed_at: { type: [string, "null"], format: date-time }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
        member_count: { type: integer, minimum: 0 }
    GradingPoolList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/GradingPool" }
    GradingPoolCreate:
      type: object
      additionalProperties: false
      required: [assessment_id, assessment_version_id, allocation_strategy]
      properties:
        assessment_id: { type: string, format: uuid }
        assessment_version_id: { type: string, format: uuid }
        allocation_strategy: { type: string, enum: [MANUAL, ROUND_ROBIN] }
    GradingPoolMember:
      type: object
      additionalProperties: false
      required: [id, pool_id, user_id, is_active]
      properties:
        id: { type: string, format: uuid }
        pool_id: { type: string, format: uuid }
        user_id: { type: string, format: uuid }
        is_active: { type: boolean }
        capacity: { type: [integer, "null"], minimum: 1 }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    GradingPoolMemberCreate:
      type: object
      additionalProperties: false
      required: [user_id]
      properties:
        user_id: { type: string, format: uuid }
        capacity: { type: [integer, "null"], minimum: 1 }
    GradingAllocateRequest:
      type: object
      additionalProperties: false
      required: [evaluation_run_id]
      properties:
        evaluation_run_id: { type: string, format: uuid }
    GradingAssignRequest:
      type: object
      additionalProperties: false
      required: [question_evaluation_id, evaluator_user_id]
      properties:
        question_evaluation_id: { type: string, format: uuid }
        evaluator_user_id: { type: string, format: uuid }
    GradingWorkItem:
      type: object
      additionalProperties: false
      required: [id, pool_id, submission_id, evaluation_run_id, question_evaluation_id, assigned_evaluator_id, status, created_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid }
        pool_id: { type: string, format: uuid }
        submission_id: { type: string, format: uuid }
        evaluation_run_id: { type: string, format: uuid }
        question_evaluation_id: { type: string, format: uuid }
        assigned_evaluator_id: { type: string, format: uuid }
        status: { type: string, enum: [QUEUED, IN_PROGRESS, SUBMITTED, RETURNED, COMPLETED] }
        assigned_by: { type: [string, "null"], format: uuid }
        assigned_at: { type: [string, "null"], format: date-time }
        started_at: { type: [string, "null"], format: date-time }
        submitted_at: { type: [string, "null"], format: date-time }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    GradingWorkItemList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/GradingWorkItem" }
    GradingPoolProgress:
      type: object
      additionalProperties: false
      required: [counts]
      properties:
        counts: { type: object, additionalProperties: { type: integer } }
        by_evaluator: { type: array, items: { type: object } }
    ModerationPolicy:
      type: object
      additionalProperties: false
      required: [id, assessment_id, assessment_version_id, status, created_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid }
        assessment_id: { type: string, format: uuid }
        assessment_version_id: { type: string, format: uuid }
        status: { type: string, enum: [DRAFT, ACTIVE, RETIRED] }
        created_by: { type: [string, "null"], format: uuid }
        activated_by: { type: [string, "null"], format: uuid }
        activated_at: { type: [string, "null"], format: date-time }
        retired_by: { type: [string, "null"], format: uuid }
        retired_at: { type: [string, "null"], format: date-time }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    ModerationPolicyList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/ModerationPolicy" }
    ModerationPolicyCreate:
      type: object
      additionalProperties: false
      required: [assessment_id, assessment_version_id, stages]
      properties:
        assessment_id: { type: string, format: uuid }
        assessment_version_id: { type: string, format: uuid }
        stages:
          type: array
          minItems: 1
          items:
            type: object
            additionalProperties: false
            required: [stage_order, required_role, label]
            properties:
              stage_order: { type: integer, minimum: 1 }
              required_role: { type: string, minLength: 1 }
              label: { type: string, minLength: 1 }
    ModerationCase:
      type: object
      additionalProperties: false
      required: [id, policy_id, submission_id, evaluation_run_id, current_stage_order, status, created_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid }
        policy_id: { type: string, format: uuid }
        submission_id: { type: string, format: uuid }
        evaluation_run_id: { type: string, format: uuid }
        current_stage_order: { type: integer, minimum: 1 }
        status: { type: string, enum: [PENDING, IN_PROGRESS, APPROVED, RETURNED, REJECTED] }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
        stages: { type: array, items: { type: object } }
        actions: { type: array, items: { type: object } }
    ModerationCaseList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/ModerationCase" }
    ModerationDecideRequest:
      type: object
      additionalProperties: false
      required: [decision]
      properties:
        decision: { type: string, enum: [APPROVE, RETURN, REJECT] }
        reason: { type: [string, "null"] }
    GrievanceCase:
      type: object
      additionalProperties: false
      required: [id, submission_id, original_published_result_id, original_evaluation_run_id, requester_reference, submitted_by, reason, status, created_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid }
        submission_id: { type: string, format: uuid }
        original_published_result_id: { type: string, format: uuid }
        original_evaluation_run_id: { type: string, format: uuid }
        requester_reference: { type: string, minLength: 1, maxLength: 255 }
        submitted_by: { type: string, format: uuid }
        reason: { type: string, minLength: 1 }
        status: { type: string, enum: [SUBMITTED, UNDER_REVIEW, ACCEPTED, REJECTED, RE_EVALUATING, RESOLVED, CLOSED] }
        decision_by: { type: [string, "null"], format: uuid }
        decision_at: { type: [string, "null"], format: date-time }
        decision_reason: { type: [string, "null"] }
        reevaluation_run_id: { type: [string, "null"], format: uuid }
        revised_published_result_id: { type: [string, "null"], format: uuid }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    GrievanceCaseList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/GrievanceCase" }
    GrievanceCreateRequest:
      type: object
      additionalProperties: false
      required: [published_result_id, requester_reference, reason]
      properties:
        published_result_id: { type: string, format: uuid }
        requester_reference: { type: string, minLength: 1, maxLength: 255 }
        reason: { type: string, minLength: 1 }
    GrievanceDecideRequest:
      type: object
      additionalProperties: false
      properties:
        decision_reason: { type: [string, "null"] }
    GrievanceResolveRequest:
      type: object
      additionalProperties: false
      required: [revised_published_result_id]
      properties:
        revised_published_result_id: { type: string, format: uuid }
